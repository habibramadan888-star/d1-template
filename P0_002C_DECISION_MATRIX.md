# P0-002C Decision Matrix

Generated: 2026-05-24, Asia/Dubai

Scope: decision freeze before any staging endpoint implementation. No live API or database schema was changed.

| Decision                 | Recommended Option                                                                                                                                               | Alternatives                                                                           | Risk                                                                                    | Blocks Implementation?            | Needs Human Approval |
| ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | --------------------------------- | -------------------- |
| Staging endpoint path    | Use `POST /api/staging/handover/commit` for P0-002C and reserve `POST /api/employee/handover/commit` for future live rollout.                                    | Use live-looking path behind feature flag; use `/api/handover/commit`.                 | A live-looking path can be accidentally treated as production-ready.                    | Yes                               | Yes                  |
| Feature flag             | Require `ENABLE_HANDOVER_ATOMIC_STAGING=true` and `APP_ENV` in `development`, `local`, or `staging`. Production returns disabled/404.                            | Feature flag only; environment only.                                                   | Single guard can fail open through config drift.                                        | Yes                               | Yes                  |
| Production behavior      | Return disabled response in production even if route exists. Prefer 404 to avoid advertising unfinished API.                                                     | 403 disabled; route absent.                                                            | Ambiguous disabled behavior can cause accidental client dependency.                     | Yes                               | Yes                  |
| Auth rule                | Employee/staff can submit only within assigned scope. Owner/admin cannot submit employee handover; they may later inspect/read.                                  | Owner/admin can submit on behalf of staff; admin replay allowed.                       | Owner submit blurs operational accountability and audit ownership.                      | Yes                               | Yes                  |
| Idempotency storage      | Use `handover_idempotency_keys` plus commit unique constraints on `handover_commits`.                                                                            | Only unique constraint on commits; memory-only rehearsal.                              | Memory-only cannot prove weak-network persistence.                                      | Yes                               | Yes                  |
| Database tables          | Use `handover_commits`, `handover_commit_rows`, `handover_idempotency_keys`, and audit bridge. Keep as staging/local migration first.                            | Reuse `sessions`/`transactions`; write only to audit logs.                             | Reusing legacy tables can mutate accounting facts before gate approval.                 | Yes                               | Yes                  |
| Money units              | Staging tables should store backend and frontend totals in integer fils while keeping display strings in response. Live switch waits for P0-001C.                | Legacy decimal only; wait for P0-001C entirely.                                        | Legacy-only staging makes later migration harder; full wait slows endpoint validation.  | Yes                               | Yes                  |
| Frontend totals mismatch | Reject the commit in staging with structured discrepancy. Do not accept as accounting fact.                                                                      | Accept with audit hold; recompute and store backend authority while flagging mismatch. | Accepting mismatch before live policy is approved can hide staff/client tampering.      | Yes                               | Yes                  |
| Weak-network retry       | Same idempotency key + same fingerprint returns replay. Same key + changed payload returns conflict. Same rows + different key returns duplicate warning/reject. | Retry by session id only.                                                              | Session-only idempotency can fail with partial batches or edited rows.                  | Yes                               | Yes                  |
| Voided compatibility     | Reject voided sessions, voided transactions, and voided row payloads from active commits. `include_voided` only for audit/report.                                | Include voided for correction; accept with warning.                                    | Recommitting voided rows can duplicate or resurrect financial facts.                    | Yes                               | Yes                  |
| Audit event storage      | For staging, write existing `audit_logs` and `entry_events` or a draft bridge table. Future production should use unified audit events.                          | Only log to console; only return event plan.                                           | No persisted audit means endpoint cannot be commercially trusted.                       | Yes                               | Yes                  |
| Receivables dependency   | P0-002C can classify and report short-pay/arrears intent but must not create live receivables until P0-008.                                                      | Block P0-002C until P0-008; implement receivables now.                                 | Creating receivables now would mix two P0 scopes.                                       | No for staging; yes for live      | Yes                  |
| Tenant dependency        | Keep endpoint local/staging and explicitly document static CORPID limitation. Reserve `company_id`/`property_id` in draft tables.                                | Wait for P0-006; ignore tenant fields.                                                 | Ignoring future scope creates migration debt; waiting blocks useful staging validation. | No for staging; yes for SaaS live | Yes                  |
| Rollback                 | Disable feature flag, keep legacy endpoint, preserve staging audit/discrepancy rows, and do not delete legacy handover logic.                                    | Drop tables; remove route.                                                             | Destructive rollback loses review evidence.                                             | Yes                               | Yes                  |
| Staging deployment       | Run local first, then staging Worker + staging D1 only. Production deployment is forbidden for P0-002C.                                                          | Deploy directly to production disabled route.                                          | Production route presence before policy approval increases operational risk.            | Yes                               | Yes                  |

## Recommended Gate Result

Proceed to P0-002C only if the human reviewer approves:

1. `POST /api/staging/handover/commit`.
2. Feature flag plus non-production environment guard.
3. Employee-only submit rule.
4. Staging/local draft migration with fils totals.
5. Reject-on-mismatch staging behavior.
6. No employee UI live cutover and no dashboard live result change.
