# Employee Entry Staging QA Guide

Generated: 2026-05-25, Asia/Dubai

Scope: manual QA guide for P0-001K. This guide is for local/staging validation only. It is not production cutover approval.

## Prerequisites

| Item                      | Required Value / Action                                                                   |
| ------------------------- | ----------------------------------------------------------------------------------------- |
| Branch                    | `review/p0-001k-employee-entry-staging-qa-gate`                                           |
| Base commit               | `57f98760b11416b2c5b29fbe658d6dc385bbab79` plus P0-001K QA files                          |
| Worker env for adapter QA | `APP_ENV=staging` or `APP_ENV=test`                                                       |
| Feature flag on           | `ENABLE_EMPLOYEE_ENTRY_ADAPTER_LIVE_ROUTE=true`                                           |
| Feature flag off rollback | `ENABLE_EMPLOYEE_ENTRY_ADAPTER_LIVE_ROUTE=false` or unset                                 |
| Production safety         | `APP_ENV=production` must keep legacy behavior even if the flag is true                   |
| Local Worker              | Start with existing local Worker scripts; do not deploy                                   |
| Employee login            | Use local/staging employee credentials only                                               |
| Owner/admin check         | Owner/admin must not submit through adapter mode                                          |
| Dashboard/history check   | Capture `/api/history` before and after each scenario and compare against expected change |
| Rollback check            | Disable the flag and submit the same class of request through legacy behavior             |

## Manual Test Cases

| Test ID   | Purpose                                  | Preconditions                            | Steps                                                                       | Request                                  | Expected Response                                        | Expected DB Evidence                                           | Expected Audit Evidence                                         | Expected Dashboard/History Behavior                        | Pass/Fail | Notes                                                                   |
| --------- | ---------------------------------------- | ---------------------------------------- | --------------------------------------------------------------------------- | ---------------------------------------- | -------------------------------------------------------- | -------------------------------------------------------------- | --------------------------------------------------------------- | ---------------------------------------------------------- | --------- | ----------------------------------------------------------------------- |
| EE-QA-001 | Production legacy behavior               | `APP_ENV=production`, flag true          | Submit valid employee entry                                                 | `POST /api/employee/entry`               | `200`, no `adapter_live_route_rehearsal`                 | Legacy `sessions` and `transactions` write as current behavior | No adapter prevalidation audit/event                            | History changes as current legacy behavior                 |           | Production adapter must remain disabled                                 |
| EE-QA-002 | Feature flag off legacy behavior         | Allowed local/staging env, flag false    | Submit valid employee entry                                                 | `POST /api/employee/entry`               | `200`, no adapter metadata                               | Legacy write only                                              | No adapter prevalidation audit/event                            | History changes as current legacy behavior                 |           | Rollback path                                                           |
| EE-QA-003 | Local/staging adapter behavior           | Allowed env, flag true, employee login   | Submit valid adapter-shaped rent entry                                      | `POST /api/employee/entry`               | `200`, `adapter_live_route_rehearsal.status=DRAFT_READY` | One legacy session and transaction by design                   | `audit_logs` and `entry_events` adapter evidence                | History changes as expected because legacy write continues |           | Not production cutover                                                  |
| EE-QA-004 | Invalid 3-decimal amount                 | Allowed env, flag true                   | Submit amount `100.999`                                                     | `POST /api/employee/entry`               | `422 EMPLOYEE_ENTRY_ADAPTER_REJECTED`                    | No legacy transaction/session write                            | Adapter rejection event exists                                  | History unchanged                                          |           | Money guardrail                                                         |
| EE-QA-005 | Empty amount                             | Allowed env, flag true                   | Submit empty amount                                                         | `POST /api/employee/entry`               | `422 EMPLOYEE_ENTRY_ADAPTER_REJECTED`                    | No legacy financial write                                      | Adapter rejection event exists                                  | History unchanged                                          |           | Money guardrail                                                         |
| EE-QA-006 | Invalid money string                     | Allowed env, flag true                   | Submit non-money string                                                     | `POST /api/employee/entry`               | Structured adapter rejection                             | No legacy financial write                                      | Adapter rejection event exists                                  | History unchanged                                          |           | Manual confirm exact error code                                         |
| EE-QA-007 | Frontend totals not authority            | Allowed env, flag true                   | Submit payload with conflicting frontend/session totals, if scenario exists | `POST /api/employee/entry`               | Manual review required                                   | No authority should come from frontend totals alone            | Evidence must show adapter/backend guardrails                   | Manual review required                                     |           | Current employee entry route lacks full handover-style total comparison |
| EE-QA-008 | Owner/admin submit denied                | Allowed env, flag true, owner/admin auth | Submit valid adapter-shaped entry                                           | `POST /api/employee/entry`               | `403 FORBIDDEN`                                          | No legacy write                                                | No successful adapter write; rejection evidence acceptable      | History unchanged                                          |           | Adapter mode employee/staff only                                        |
| EE-QA-009 | Rollback by flag off                     | Allowed env, flag false                  | Submit valid entry after an adapter-on run                                  | `POST /api/employee/entry`               | `200`, no adapter metadata                               | Legacy write only                                              | No adapter prevalidation audit/event                            | History changes as legacy                                  |           | Confirms rollback                                                       |
| EE-QA-010 | Dashboard unchanged for no-write cases   | Allowed env, flag true                   | Run invalid, owner/admin, and voided cases                                  | `POST /api/employee/entry`               | Rejected or skipped                                      | No legacy financial write                                      | Adapter evidence exists                                         | History unchanged                                          |           | Use no-write cases for unchanged assertion                              |
| EE-QA-011 | History expected change for valid writes | Allowed env, flag true                   | Submit valid adapter-shaped entry                                           | `POST /api/employee/entry`               | `200`                                                    | One session/transaction write                                  | Adapter evidence exists                                         | History changes exactly because legacy write continues     |           | Not an unexpected dashboard change                                      |
| EE-QA-012 | delete_session void still works          | Local/staging env                        | Run delete session void regression                                          | `npm run test:delete-session`            | PASS                                                     | Rows preserved and voided                                      | Audit evidence exists                                           | Active history excludes voided                             |           | Regression                                                              |
| EE-QA-013 | Handover staging endpoint still works    | Local/staging env                        | Run handover staging endpoint regression                                    | `npm run test:handover-staging-endpoint` | PASS                                                     | Staging tables only                                            | Staging audit evidence                                          | Live history unchanged for staging handover                |           | Regression                                                              |
| EE-QA-014 | Reconciliation gate review               | Local D1                                 | Run reconciliation gate                                                     | `npm run gate:money-reconciliation`      | `MANUAL_REQUIRED`, no FAIL/BLOCKED                       | Missing future fils fields expected                            | N/A                                                             | N/A                                                        |           | Production cutover NO-GO                                                |
| EE-QA-015 | Audit evidence exists                    | Allowed env, flag true                   | Submit valid and invalid adapter cases                                      | `POST /api/employee/entry`               | Valid/invalid expected statuses                          | Valid writes expected rows; invalid writes none                | `employee.entry.adapter_prevalidation` and entry event evidence | Expected per case                                          |           | Required for traceability                                               |

## Required Automated Commands

```text
npm run test:employee-entry-route-switch
npm run compare:employee-entry-routes
npm run rehearse:employee-entry-rollback
npm run test:employee-entry-production-lock
npm run verify:dashboard-unchanged
npm run verify:handover-legacy-unchanged
npm run gate:money-reconciliation
npm run security:secrets
```

## Production Cutover Warning

Passing this guide means GO for real staging QA only. It does not mean production cutover is allowed.
