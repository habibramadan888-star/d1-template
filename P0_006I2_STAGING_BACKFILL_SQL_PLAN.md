# P0-006I2 Staging Backfill SQL Plan

Date: 2026-05-26, Asia/Dubai

Scope: staging-only compatibility-column backfill for
`homelink-finance-staging`. Every planned `UPDATE` has a restrictive `WHERE`
clause and only updates compatibility scope columns.

| Step | SQL                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Target Table   | Estimated Rows | Rollback                                                                       |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------- | -------------: | ------------------------------------------------------------------------------ |
| 1    | `UPDATE sessions SET company_id='homelink-staging', property_id='HL-STAGING-QA', employee_id=operator_id WHERE corpid='homelink-staging' AND source='EMP' AND handover_status='STAGING_QA' AND operator_id='employee_stg_qa_001' AND created_by='employee_stg_qa_001' AND id LIKE 'stg-ee-session-%' AND (company_id IS NULL OR property_id IS NULL OR employee_id IS NULL);`                                                                                                                                                                                                                                                                    | `sessions`     |              1 | Restore backup or inverse update by same `WHERE` plus reviewed row id.         |
| 2    | `UPDATE transactions SET company_id=(SELECT s.company_id FROM sessions s WHERE s.id=transactions.session_id LIMIT 1), property_id=(SELECT s.property_id FROM sessions s WHERE s.id=transactions.session_id LIMIT 1), employee_id=COALESCE(operator_id, userid) WHERE corpid='homelink-staging' AND id LIKE 'stg-ee-entry-%' AND session_id IN (SELECT id FROM sessions WHERE corpid='homelink-staging' AND source='EMP' AND handover_status='STAGING_QA' AND company_id IS NOT NULL AND property_id IS NOT NULL AND employee_id IS NOT NULL) AND (company_id IS NULL OR property_id IS NULL OR employee_id IS NULL);`                            | `transactions` |              1 | Restore backup or inverse update by same `WHERE` plus reviewed transaction id. |
| 3    | `UPDATE entry_events SET company_id=(SELECT t.company_id FROM transactions t WHERE t.id=entry_events.ref_id LIMIT 1), property_id=(SELECT t.property_id FROM transactions t WHERE t.id=entry_events.ref_id LIMIT 1), employee_id=COALESCE(operator_id, userid) WHERE corpid='homelink-staging' AND ref_type IN ('transaction','employee_entry_live_route_switch_rehearsal') AND ref_id IN (SELECT id FROM transactions WHERE corpid='homelink-staging' AND company_id IS NOT NULL AND property_id IS NOT NULL AND employee_id IS NOT NULL) AND (company_id IS NULL OR property_id IS NULL OR employee_id IS NULL);`                              | `entry_events` |              2 | Restore backup or inverse update by reviewed `event_id` list.                  |
| 4    | `UPDATE entry_events SET company_id=(SELECT hc.company_id FROM handover_commits hc WHERE hc.commit_id=entry_events.ref_id LIMIT 1), property_id=(SELECT hc.property_id FROM handover_commits hc WHERE hc.commit_id=entry_events.ref_id LIMIT 1), employee_id=(SELECT hc.employee_id FROM handover_commits hc WHERE hc.commit_id=entry_events.ref_id LIMIT 1) WHERE corpid='homelink-staging' AND ref_type='handover_commit' AND ref_id IN (SELECT commit_id FROM handover_commits WHERE company_id IS NOT NULL AND property_id IS NOT NULL AND employee_id IS NOT NULL) AND (company_id IS NULL OR property_id IS NULL OR employee_id IS NULL);` | `entry_events` |              1 | Restore backup or inverse update by reviewed `event_id` list.                  |
| 5    | `UPDATE audit_logs SET company_id=(SELECT t.company_id FROM transactions t WHERE t.id=audit_logs.target LIMIT 1), property_id=(SELECT t.property_id FROM transactions t WHERE t.id=audit_logs.target LIMIT 1), employee_id=COALESCE(userid, (SELECT t.employee_id FROM transactions t WHERE t.id=audit_logs.target LIMIT 1)) WHERE corpid='homelink-staging' AND target IN (SELECT id FROM transactions WHERE corpid='homelink-staging' AND company_id IS NOT NULL AND property_id IS NOT NULL AND employee_id IS NOT NULL) AND (company_id IS NULL OR property_id IS NULL OR employee_id IS NULL);`                                             | `audit_logs`   |              2 | Restore backup or inverse update by reviewed `id` list.                        |
| 6    | `UPDATE audit_logs SET company_id=(SELECT hc.company_id FROM handover_commits hc WHERE hc.commit_id=audit_logs.target LIMIT 1), property_id=(SELECT hc.property_id FROM handover_commits hc WHERE hc.commit_id=audit_logs.target LIMIT 1), employee_id=(SELECT hc.employee_id FROM handover_commits hc WHERE hc.commit_id=audit_logs.target LIMIT 1) WHERE corpid='homelink-staging' AND target IN (SELECT commit_id FROM handover_commits WHERE company_id IS NOT NULL AND property_id IS NOT NULL AND employee_id IS NOT NULL) AND (company_id IS NULL OR property_id IS NULL OR employee_id IS NULL);`                                        | `audit_logs`   |              1 | Restore backup or inverse update by reviewed `id` list.                        |

SQL safety review:

- Contains `UPDATE`: yes, approved staging backfill only.
- Contains `WHERE` on every `UPDATE`: yes.
- Contains `INSERT`: no.
- Contains `DELETE`: no.
- Contains `DROP`: no.
- Updates legacy `corpid`: no.
- Updates financial amount fields: no.
- Updates dashboard logic: no.
- Production target: no.
