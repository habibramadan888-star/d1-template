# P0-001K Review of P0-001J Employee Entry Route Switch

Generated: 2026-05-25, Asia/Dubai

Scope: review of commit `57f98760b11416b2c5b29fbe658d6dc385bbab79` and the guarded `/api/employee/entry` rehearsal path. This review did not deploy, run production or remote migrations, change production config, delete legacy code, or submit secrets.

## Summary

| Question                                                 | Answer                                                                                                                                                                                                     | Evidence                                                                                              |
| -------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| What live code path changed?                             | `handleEmployeeEntry` in `deploy-worker/src/index.js` now performs adapter pre-validation only when `ENABLE_EMPLOYEE_ENTRY_ADAPTER_LIVE_ROUTE` is enabled in an allowed non-production environment.        | `deploy-worker/src/index.js`, `tests/employee-entry-route-switch-rehearsal.spec.mjs`                  |
| How does production continue legacy?                     | `eeaLiveRouteGate` returns disabled unless the app env is one of development/dev/local/test/staging. `APP_ENV=production` is not allowed, so the route continues the legacy path without adapter metadata. | `npm run test:employee-entry-production-lock`                                                         |
| How does feature flag off continue legacy?               | If `ENABLE_EMPLOYEE_ENTRY_ADAPTER_LIVE_ROUTE` is absent/false, adapter pre-validation is skipped and the legacy write path runs.                                                                           | `EMPLOYEE_ENTRY_ROUTE_SWITCH_ROLLBACK_RESULT.md`                                                      |
| How does local/staging flag on enter adapter?            | Allowed env plus flag on runs `createEmployeeEntryLiveWriteAdapterDraft(eeaBuildAdapterInput(...))` before legacy writes.                                                                                  | `EMPLOYEE_ENTRY_ROUTE_SWITCH_REHEARSAL_RESULT.md`                                                     |
| Is rollback available?                                   | Yes. Turning the flag off returns to legacy behavior with no adapter metadata.                                                                                                                             | `EMPLOYEE_ENTRY_ROLLBACK_DRILL_RESULT.md`                                                             |
| Does adapter use money dual-write/minor-unit guardrails? | Yes. It produces `*_fils` patch plans and rejects unsafe money before legacy write. It does not itself write the database.                                                                                 | `modules/worker/employee-entry-live-write-adapter.mjs`, `EMPLOYEE_ENTRY_ROUTE_SWITCH_SAFETY_AUDIT.md` |
| Could adapter change dashboard unexpectedly?             | Valid adapter rehearsals intentionally continue legacy writes, so history changes exactly as legacy writes do. Invalid, owner/admin, and voided adapter cases do not write legacy financial rows.          | `EMPLOYEE_ENTRY_LEGACY_VS_ADAPTER_COMPARISON.md`                                                      |
| Does adapter change live financial formula?              | No production formula is replaced. The adapter performs pre-validation and then continues the existing legacy calculation path for accepted rows.                                                          | `deploy-worker/src/index.js`                                                                          |
| Does adapter write legacy financial tables?              | The adapter module does not write tables. The accepted local/staging route continues the current legacy path, which writes existing `sessions` and `transactions` rows by design.                          | `EMPLOYEE_ENTRY_LEGACY_VS_ADAPTER_COMPARISON.md`                                                      |
| Does valid adapter write match rehearsal design?         | Yes. Valid adapter case returns `DRAFT_READY`, writes one legacy transaction/session, and records adapter audit/entry evidence.                                                                            | `npm run compare:employee-entry-routes`                                                               |
| Do invalid / voided / owner/admin cases avoid writes?    | Yes. Invalid money and owner/admin are rejected before legacy write; voided rows are skipped before legacy write.                                                                                          | `npm run test:employee-entry-route-switch`                                                            |
| Is there hidden production behavior change?              | No evidence found. Production lock test verifies production remains legacy for flag true and flag false.                                                                                                   | `npm run test:employee-entry-production-lock`                                                         |
| Which code needs human review?                           | `handleEmployeeEntry` gate/pre-validation block, adapter error handling, and accepted adapter continuation into legacy write path.                                                                         | `deploy-worker/src/index.js`                                                                          |

## Human Review Notes

| Area                        | Required Review                                                                                                                                                                    |
| --------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Valid adapter continuation  | Confirm that local/staging accepted adapter rows should continue to the legacy write path until production cutover.                                                                |
| Legacy money fields         | `transactions`, `sessions`, and related tables still contain legacy decimal/REAL fields. Production cutover remains blocked until minor-unit migration/backfill is approved.       |
| Owner/admin legacy behavior | Legacy path still allows owner submit in some test paths. Adapter rehearsal denies owner/admin only when flag is enabled. Confirm desired production behavior before live cutover. |
| Voided legacy behavior      | Legacy can still treat voided-looking input differently than adapter. Adapter skips voided rows before legacy write. Confirm desired final production policy.                      |
| Reconciliation              | `gate:money-reconciliation` remains `MANUAL_REQUIRED`; production cutover is still NO-GO.                                                                                          |
