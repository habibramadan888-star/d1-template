# ARREARS COUNT/LIST MISMATCH ROOT CAUSE

Status: fixed in code, pending user screenshot confirmation.

| Problem                                                                            | File / Selector                                                             | Root Cause                                                                                                                                                      | Required Fix                                                                                                                        |
| ---------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| Summary showed total followup count while visible list showed only about 5 cards   | `deploy-worker/public/index-51-main.js` / `renderOwnerOverviewArrearsPanel` | Overview intentionally rendered `ARREARS_OVERVIEW_PAGE_SIZE` preview rows, but did not show `visible / total` and the button label did not include total count. | Add explicit visible and total data attributes plus `preview N / total M` UI text.                                                  |
| `查看全部` looked inactive                                                         | `deploy-worker/public/index-51-main.js` / `toggleOverviewArrearsAll`        | Expand action could start a second load path and return before a local re-render.                                                                               | Make expand/collapse a deterministic local action over already loaded `state.arrears`, then render both overview and arrears panel. |
| API contract did not distinguish summary, preview, and complete list               | `deploy-worker/src/index.js` / `handleBossArrearsFollowupTasks`             | Endpoint returned `tasks` and `recent_tasks`; frontend had to infer which list was authoritative.                                                               | Return `summary`, `preview_tasks`, `all_tasks`, `sources`, `has_more` while preserving existing fields.                             |
| Existing/system arrears could appear lost when TTLock rows were mixed into `tasks` | `deploy-worker/public/index-51-main.js` / `loadArrearsForOwner`             | Frontend split `rawExistingRows` but only consumed legacy keys, so future explicit `all_tasks` contract was ignored.                                            | Prefer `all_tasks`, then split source rows by `source_type`.                                                                        |
| Backend TTLock rows could be re-filtered by frontend rent config                   | `deploy-worker/public/index-51-main.js` / `buildArrearsFollowupPool`        | Backend rows already include bed-rent-mapped amounts, but frontend still required local room config.                                                            | Accept backend TTLock rows when `amount_source` / `amount_authority_status` proves bed-rent mapping.                                |

Conclusion: the visible mismatch was a contract and UI-state problem, not a financial calculation change.
