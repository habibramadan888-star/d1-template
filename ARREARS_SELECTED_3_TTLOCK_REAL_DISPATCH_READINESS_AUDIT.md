# Selected 3 TTLock Real Dispatch Readiness Audit

Date: 2026-06-01, Asia/Dubai

Scope: readiness audit for the user-selected TTLock expired unpaid rooms/beds `112`, `113`, and `125`.

## Read-Only Candidate Snapshot

| Room / Bed | Task ID | Amount | Source | Directive Status | Already Active Directive | Stable For Write | Ready For Write | Blocker |
|---|---|---:|---|---|---|---|---|---|
| 112 | `ttlock-expired-139777220` | 770.00 AED | `ttlock_expired_unpaid` | `none` | no | no | no | TTLock row is generated by read-only aggregation and is not proven persisted in `arrear_tasks`. |
| 113 | `ttlock-expired-139777824` | 700.00 AED | `ttlock_expired_unpaid` | `none` | no | no | no | TTLock row is generated by read-only aggregation and is not proven persisted in `arrear_tasks`. |
| 125 | `ttlock-expired-139778918` | 700.00 AED | `ttlock_expired_unpaid` | `none` | no | no | no | TTLock row is generated by read-only aggregation and is not proven persisted in `arrear_tasks`. |

## Backend Contract Finding

The owner read path maps TTLock expired unpaid cards into virtual arrears rows using IDs like `ttlock-expired-<sourceRef>`.

The real write path for `POST /api/boss/arrears/directives` only updates existing rows from:

```sql
SELECT * FROM arrear_tasks WHERE task_id=? AND corpid=? LIMIT 1
```

and then:

```sql
UPDATE arrear_tasks SET ... directive_status='assigned' WHERE task_id=? AND corpid=?
```

There is no confirmed production materialization path that inserts or upserts selected TTLock virtual rows into `arrear_tasks` before assigning them.

## Conclusion

| Check | Result |
|---|---|
| Selected task count is exactly 3 | PASS |
| Rooms/beds are exactly 112, 113, 125 | PASS |
| Source is TTLock expired unpaid | PASS |
| Amount exists from bed-rent mapping | PASS |
| Active directive absent in read model | PASS |
| Write API can safely persist selected virtual TTLock rows | FAIL |
| Readiness conclusion | `BLOCKED_TASK_ID_UNSTABLE` |
| Related blocker | `BLOCKED_SCHEMA_MISSING` |
| Production write gate opened | no |
| Production D1 write | no |
| Owner directive create attempted | no |
| Employee follow-up attempted | no |
| Production cutover | `PRODUCTION_NO_GO` |

## Required Fix Before Real Dispatch

Implement and test a dedicated TTLock directive materialization path before production write:

1. Persist selected TTLock expired unpaid rows into `arrear_tasks`, or extend the write endpoint to materialize them transactionally.
2. Preserve amount authority as bed-rent mapping.
3. Preserve idempotency and audit evidence.
4. Re-run staging TTLock E2E.
5. Request separate approval before opening production write gate.
