# Handover Flow Audit

Generated: 2026-05-24, Asia/Dubai

Scope: P0-002A audit only. This document does not change employee UI, Worker routes, database schema, production configuration, or financial formulas.

| Step                             | File                                                                  | Current Behavior                                                                                                                                            | Risk                                                                                                           | Required Future Behavior                                                                                  |
| -------------------------------- | --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| 1. Employee builds local session | `deploy-worker/public/employee-v3.html`                               | Browser keeps `state.drafts`, `sessionId`, and local summary state.                                                                                         | Drafts can exist locally without a single backend accepted handover state.                                     | Keep local drafts, but submit the final handover as one backend commit payload.                           |
| 2. Employee row submission       | `deploy-worker/public/employee-v3.html` -> `POST /api/employee/entry` | Rows are uploaded individually; each call can create/update `sessions`, `transactions`, `arrear_tasks`, `deposit_ledger`, `entry_events`, and `audit_logs`. | Partial network success can create incomplete handover; session totals can be inconsistent with accepted rows. | Backend should accept all final rows atomically or reject the whole handover.                             |
| 3. Session totals                | `deploy-worker/public/employee-v3.html`, `deploy-worker/src/index.js` | Browser computes `cash_handover`, `bank_transfer_total`, `bank_transfer_count`, `gross_received`; Worker stores legacy session totals.                      | Frontend can become accounting authority.                                                                      | Backend recomputes totals from accepted rows and stores authoritative integer-fils totals.                |
| 4. Export text                   | `deploy-worker/public/employee-v3.html`                               | Text export is built in browser from local session state.                                                                                                   | Export may not match backend state if some rows failed or were duplicated.                                     | Backend commit response should return accepted summary/export metadata or a signed reconciliation result. |
| 5. Duplicate submit              | Existing entry idempotency modules                                    | Per-entry idempotency exists in future modules, but final handover batch idempotency is not live.                                                           | Staff can retry export and create partial duplicates.                                                          | Add `idempotency_key` for the entire handover commit plus per-row client ids.                             |
| 6. Owner legacy save             | `POST /api/save_session`                                              | Owner legacy path inserts session/transactions separately and lacks backend-owned handover validation.                                                      | Legacy path remains outside employee atomic handover guarantee.                                                | Keep separate and plan later migration; do not mix owner legacy imports with employee handover commit.    |
| 7. Void compatibility            | `POST /api/delete_session`                                            | P0-004 voids session-linked rows and preserves audit evidence.                                                                                              | Atomic commit must remain void-compatible.                                                                     | New commit should create rows with consistent session id so void can mark all linked records.             |

## Conclusion

Current employee handover is operationally usable but not commercially atomic. P0-002 remains Partial until a server-side handover commit endpoint writes all rows, recomputes totals, records audit events, and handles retries idempotently.
