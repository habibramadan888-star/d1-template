# Backend Totals Authority Audit

Generated: 2026-05-24, Asia/Dubai

Scope: P0-003A audit and shadow recomputation only. No production dashboard total, handover behavior, API response, database schema, or financial formula was changed.

## Authority Map

| Area                      | Frontend Total                                                                                                                        | Backend Total                                                                                                                               | Source Of Truth                                                                           | Risk                                                                                                  | Recommendation                                                                                               |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| Employee session handover | `employee-v3.html` builds `cash_handover`, `bank_transfer_total`, `bank_transfer_count`, `gross_received` from browser session state. | Legacy Worker currently accepts these session fields and stores them on `sessions`.                                                         | Future source of truth must be backend recompute from accepted transaction rows.          | Staff browser can become accounting authority; weak-network or tampered totals can diverge from rows. | Keep current route unchanged now; add backend shadow comparison and later reject/recompute in P0-003B.       |
| Cash handover             | Frontend summary treats cash balance as cash income minus cash out.                                                                   | `modules/finance/handover.mjs` can compute `cashHandoverFils` in integer fils for isolated tests.                                           | Backend recompute should use accepted row event type, payment method, and integer amount. | Legacy `sessions.cash_handover` is `REAL` and may be submitted directly.                              | Use `computeHandoverSummary`/shadow helper to compare submitted and recomputed values before live migration. |
| Bank transfer total/count | Frontend stores bank total and count for handover.                                                                                    | Isolated commercial write plan and D1 executor already include `bank_transfer_total_fils` and `bank_transfer_count` recomputation evidence. | Backend should count accepted bank income rows only.                                      | Browser count can be wrong or duplicated.                                                             | P0-003B should recompute bank total/count server-side and return mismatch evidence.                          |
| Gross received            | Frontend gross received means total collected income before cash-out deductions.                                                      | `computeHandoverSummary` calculates `grossReceivedFils` from income categories.                                                             | Backend row aggregation.                                                                  | Frontend formula or category mapping can drift from accounting policy.                                | Keep gross received definition tied to event categories in backend helper/tests.                             |
| Owner dashboard           | Owner UI may reduce history/session rows locally with `Number`, `parseFloat`, `Math.round`.                                           | Current owner APIs return legacy rows; not all dashboard totals are backend authority.                                                      | Future owner dashboard should consume backend-computed summaries.                         | UI math can mislead management even if database rows are intact.                                      | P0-003C should expose backend summary endpoints and golden-data comparisons.                                 |
| Delete-session void       | Frontend/owner action voids a session.                                                                                                | P0-004 preserves rows and marks void fields; active reads filter voided rows.                                                               | Void rows remain audit history; active totals should exclude voided rows.                 | Recompute must consistently exclude voided rows or dashboard totals can revive voided data.           | Add void-aware total tests when backend totals become live.                                                  |

## Current Conclusion

P0-003 is not fixed. The project has isolated backend recompute helpers and rehearsal evidence, but the live Worker route can still accept frontend-provided handover totals. This stage prepares shadow comparison only.
