# Money Precision Audit Result

Generated: 2026-05-24, Asia/Dubai

Scope: static source scan for P0-001A. This report is non-blocking and does not modify business calculations.

## Summary

| Metric                                                         | Count |
| -------------------------------------------------------------- | ----: |
| REAL / FLOAT / NUMERIC / DECIMAL risks                         |   188 |
| JS Number / parseFloat / parseInt / toFixed / Math.round risks |   467 |
| Frontend amount calculation risks                              |   435 |
| Backend amount calculation risks                               |   144 |
| Total money-related findings scanned                           |  2625 |

## Notes

- This scan intentionally includes legacy Worker and frontend code because P0-001A is an inventory task.
- Counts are risk indicators, not proof that every occurrence is an active production accounting defect.
- P0-001 remains Partial because this task does not migrate storage or live write paths to integer minor units.

## Detailed Findings

| Area     | File                                           | Kind                 | Evidence                                                                                                                                                                                                               | Recommendation                                                                            |
| -------- | ---------------------------------------------- | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| Other    | `deploy-worker/employee-patch-fragment.js:3`   | money keyword        | `"id","corpid","userid","session_id","cat","room","amount","due","paid","deficit","tag","note","room_to",`                                                                                                             | Keep documented as legacy risk unless guarded by integer-fils tests.                      |
| Other    | `deploy-worker/employee-patch-fragment.js:48`  | schema floating type | `arrear_amount REAL,`                                                                                                                                                                                                  | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Other    | `deploy-worker/employee-patch-fragment.js:53`  | schema floating type | `promise_amount REAL,`                                                                                                                                                                                                 | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Other    | `deploy-worker/employee-patch-fragment.js:54`  | schema floating type | `actual_received REAL DEFAULT 0,`                                                                                                                                                                                      | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Other    | `deploy-worker/employee-patch-fragment.js:122` | JS number operation  | `const amount=Number(entry.amount\|\|0);`                                                                                                                                                                              | Keep documented as legacy risk unless guarded by integer-fils tests.                      |
| Other    | `deploy-worker/employee-patch-fragment.js:124` | money keyword        | `if(!room\|\|!Number.isFinite(amount)\|\|amount<=0)return badRequest("room_amount_required");`                                                                                                                         | Keep documented as legacy risk unless guarded by integer-fils tests.                      |
| Other    | `deploy-worker/employee-patch-fragment.js:134` | money keyword        | `id:entryId,corpid:user.corpid,userid:user.userid,session_id:sessionId,cat:cleanText(entry.cat\|\|"cash",20),`                                                                                                         | Keep documented as legacy risk unless guarded by integer-fils tests.                      |
| Other    | `deploy-worker/employee-patch-fragment.js:135` | money keyword        | `room,amount,tag:cleanText(entry.tag\|\|"Old",20),note:cleanText(entry.note,500),room_to:cleanText(entry.roomTo\|\|entry.room_to,40),`                                                                                 | Keep documented as legacy risk unless guarded by integer-fils tests.                      |
| Other    | `deploy-worker/employee-patch-fragment.js:140` | money keyword        | `await empEvent(env,user,{ref_id:entryId,ref_type:"transaction",event_type:"create",field_name:"*",new_value:JSON.stringify(entry),operator_id:entry.operator_id\|\|user.userid,ts:now}`                               | Keep documented as legacy risk unless guarded by integer-fils tests.                      |
| Other    | `deploy-worker/employee-patch-fragment.js:141` | money keyword        | `await audit(env,user,"employee.entry.create",entryId,{room,amount}).catch(()=>{});`                                                                                                                                   | Keep documented as legacy risk unless guarded by integer-fils tests.                      |
| Other    | `deploy-worker/employee-patch-fragment.js:149` | money keyword        | `if(!tasks.length && await empTableExists(env,"arrears")){`                                                                                                                                                            | Keep documented as legacy risk unless guarded by integer-fils tests.                      |
| Other    | `deploy-worker/employee-patch-fragment.js:150` | money keyword        | `const ar=await env.DB.prepare("SELECT * FROM arrears WHERE corpid=? AND cleared=0 ORDER BY created_at DESC").bind(user.corpid).all();`                                                                                | Keep documented as legacy risk unless guarded by integer-fils tests.                      |
| Other    | `deploy-worker/employee-patch-fragment.js:165` | money keyword        | `const fallback=await env.DB.prepare("SELECT * FROM arrears WHERE id=? AND corpid=? LIMIT 1").bind(taskId,user.corpid).first().catch(()=>null);`                                                                       | Keep documented as legacy risk unless guarded by integer-fils tests.                      |
| Other    | `deploy-worker/employee-patch-fragment.js:168` | JS number operation  | `tenant_name:patch.tenant_name\|\|fallback?.tenant_name\|\|"",arrear_amount:Number(fallback?.remain\|\|patch.arrear_amount\|\|0),`                                                                                     | Keep documented as legacy risk unless guarded by integer-fils tests.                      |
| Frontend | `deploy-worker/public/employee-v2.html:74`     | money keyword        | `<button class="tab" data-view="arrears">欠款<span class="en-sub">ARREARS</span></button>`                                                                                                                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:94`     | schema floating type | `<div class="field"><label>实收金额 amount</label><input id="amount" inputmode="decimal" placeholder="400.00"></div>`                                                                                                  | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v2.html:98`     | schema floating type | `<div class="field"><label>应收金额 due</label><input id="due" inputmode="decimal" placeholder="系统参考或手动填写"></div>`                                                                                            | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v2.html:99`     | schema floating type | `<div class="field"><label>计入金额 paid</label><input id="paid" inputmode="decimal" placeholder="默认等于实收"></div>`                                                                                                | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v2.html:107`    | schema floating type | `<div class="field hidden" id="customDaysWrap"><label>自定义天数</label><input id="customDays" inputmode="numeric" placeholder="例如 10"></div>`                                                                       | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v2.html:121`    | money keyword        | `<section id="view-arrears" class="hidden">`                                                                                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:145`    | JS number operation  | `const money=n=>(Number(n)\|\|0).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});`                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:155`    | JS number operation  | `function addDays(dateStr,days){const d=new Date(dateStr+'T00:00:00');d.setDate(d.getDate()+Number(days\|\|0));return `${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`}`                                | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:156`    | JS number operation  | `function addMonths(dateStr,months){const d=new Date(dateStr+'T00:00:00');const day=d.getDate();d.setMonth(d.getMonth()+Number(months\|\|0));if(d.getDate()!==day)d.setDate(0);return ``                               | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:163`    | JS number operation  | `if(md&&validMD(Number(md[1]),Number(md[2]))){const y=new Date().getFullYear();return `${y}-${pad(md[1])}-${pad(md[2])}`;}`                                                                                            | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:166`    | JS number operation  | `const m=Number(compact[1]),d=Number(compact[2]);`                                                                                                                                                                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:183`    | JS number operation  | `const s=String(Math.trunc(Number(n)\|\|0)).padStart(4,'0');`                                                                                                                                                          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:184`    | JS number operation  | `const m=Number(s.slice(0,2)),d=Number(s.slice(2));`                                                                                                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:196`    | JS number operation  | `const remarkNums=[...remarkText.matchAll(/\b(\d{2,5}(?:\.\d{1,2})?)\b/g)].map(m=>Number(m[1]));`                                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:197`    | JS number operation  | `const allNums=[...text.matchAll(/\b(\d{2,5}(?:\.\d{1,2})?)\b/g)].map(m=>Number(m[1]));`                                                                                                                               | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:198`    | JS number operation  | `const deposit=depLabel?Number(depLabel[1]):(`                                                                                                                                                                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:199`    | JS number operation  | `remarkNums.find(n=>n!==Number(bed)&&n>=100&&n<=5000&&!numberLooksLikeMonthDay(n))\|\|`                                                                                                                                | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:200`    | JS number operation  | `allNums.find(n=>n!==Number(bed)&&n>=100&&n<=5000&&!numberLooksLikeMonthDay(n))\|\|`                                                                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:205`    | money keyword        | `return {bed,room:roomText,searchText:text,tenant:raw.tenant_name\|\|raw.tenant\|\|raw.cardName\|\|raw.name\|\|'',deposit,start,end:end&&!isNaN(end)?`${end.getFullYear()}-${pad(end.getMonth`                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:223`    | money keyword        | `['押金',c&&c.deposit?money(c.deposit):'未抓取'],`                                                                                                                                                                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:236`    | money keyword        | `if(c.deposit&&!$('amount').value)$('amount').value=money(c.deposit);`                                                                                                                                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:261`    | money keyword        | `const cat=type==='DR'?'refund':type==='E'?'expense':type==='R'?'cash':'bank';`                                                                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:265`    | JS number operation  | `const amount=Number($('amount').value\|\|0);`                                                                                                                                                                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:266`    | JS number operation  | `const due=Number($('due').value\|\|amount\|\|0);`                                                                                                                                                                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:267`    | JS number operation  | `const paid=Number($('paid').value\|\|amount\|\|0);`                                                                                                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:269`    | money keyword        | `id:uid('ent'),cat,room:$('bed').value.trim().replace(/^#/,''),amount,due,paid,deficit:Math.max(0,due-paid),`                                                                                                          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:270`    | money keyword        | `tag:type==='D'?'New':type==='TF'?'Transfer':'Old',note:$('note').value.trim(),`                                                                                                                                       | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:281`    | money keyword        | `const acc=` due:${money(e.due)} paid:${money(e.paid)} clr:${e.clr\|\|'Y'}`;`                                                                                                                                          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:284`    | money keyword        | `return `#${e.room}${move} ${money(e.amount)} ${code}${p}${acc}${reason}${tenant} 员工:${e.operator_id}${note}`;`                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:288`    | money keyword        | `if(!e.room\|\|!e.amount){toast('请填写床位和金额');return}`                                                                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:292`    | money keyword        | `id:e.id,cat:e.cat,room:e.room,amount:e.amount,due:e.due,paid:e.paid,deficit:e.deficit,tag:e.tag,note:e.note,roomTo:e.roomTo,`                                                                                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:298`    | money keyword        | `if(!r.ok)r=await apiFetch('/api/save_session',{method:'POST',body:JSON.stringify({session,arrears:[]})});`                                                                                                            | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:302`    | money keyword        | `function resetForm(){['bed','roomTo','amount','due','paid','tenantName','periodStart','periodEnd','customDays','reason','note'].forEach(id=>$(id).value='');$('clr').value='Y';state`                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:311`    | money keyword        | `r=await apiFetch('/api/arrears');`                                                                                                                                                                                    | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:314`    | money keyword        | `data=(Array.isArray(rows)?rows:rows.arrears\|\|[]).filter(x=>!x.cleared).map(x=>({`                                                                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:328`    | money keyword        | `<div class="task-top"><div><div class="bed">#${esc(t.bed\|\|'')}</div><div class="small">${esc(t.tenant_name\|\|'未填租客')} · ${esc(t.arrear_reason\|\|'欠款')}</div></div><div class="money">A`                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:331`    | schema floating type | `<div class="grid2"><div class="field"><label>承诺金额</label><input inputmode="decimal" data-k="promise_amount" value="${esc(t.promise_amount\|\|'')}"></div><div class="field"><label>实际收`                        | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v2.html:356`    | money keyword        | `const updates=state.tasks.filter(t=>t.updated_at).map(t=>`#${t.bed} arrear:${money(t.arrear_amount)} status:${t.followup_status} promise:${t.promise_date\|\|'-'} promise_amt:${money(`                               | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:360`    | money keyword        | `document.querySelectorAll('.tab').forEach(btn=>btn.onclick=()=>{document.querySelectorAll('.tab').forEach(b=>b.classList.remove('active'));btn.classList.add('active');['entry','arr`                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:364`    | money keyword        | `$('amount').oninput=()=>{if(!$('paid').value)$('paid').value=$('amount').value;if(!$('due').value)$('due').value=$('amount').value;};`                                                                                | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:365`    | JS number operation  | `$('due').oninput=()=>{const due=Number($('due').value\|\|0),paid=Number($('paid').value\|\|$('amount').value\|\|0);$('clr').value=paid>=due?'Y':'N';};`                                                               | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v2.html:366`    | JS number operation  | `$('paid').oninput=()=>{const due=Number($('due').value\|\|$('amount').value\|\|0),paid=Number($('paid').value\|\|0);$('clr').value=paid>=due?'Y':'N';};`                                                              | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:133`    | money keyword        | `text-wrap:balance;`                                                                                                                                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:230`    | schema floating type | `.kpi-card b,.ctx b,.summary b,.preview-entry .amt,.bed,.money,input[inputmode="decimal"],#periodDays,#periodDue,#due,#paid,#listPrice,#depositDeduction,#depositReturnAmount,#overda`                                 | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v3.html:232`    | schema floating type | `font-variant-numeric:tabular-nums;`                                                                                                                                                                                   | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v3.html:259`    | schema floating type | `.kpi-card b,.ctx b,.summary b,.preview-entry .amt,.bed,.money,input[inputmode="decimal"],#periodDays,#periodDue,#due,#paid,#listPrice,#depositDeduction,#depositReturnAmount,#overda`                                 | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v3.html:261`    | schema floating type | `font-variant-numeric:tabular-nums!important;`                                                                                                                                                                         | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v3.html:416`    | money keyword        | `.session-kpi-grid .kpi-card.anchor.bank b{color:#1a73e8!important}`                                                                                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:417`    | money keyword        | `.session-kpi-grid .kpi-card.anchor.total b{color:#142033!important}`                                                                                                                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:795`    | money keyword        | `.arrear-plan{`                                                                                                                                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:802`    | money keyword        | `.arrear-plan .plan-head{`                                                                                                                                                                                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:811`    | money keyword        | `.arrear-plan .plan-head small{`                                                                                                                                                                                       | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:818`    | money keyword        | `.arrear-plan .plan-amount{`                                                                                                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:825`    | money keyword        | `.amount-hint{`                                                                                                                                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:863`    | money keyword        | `.priority-card.arrears-check,`                                                                                                                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:867`    | money keyword        | `.priority-card.deposit-check,`                                                                                                                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:868`    | money keyword        | `.priority-card.rent-check{`                                                                                                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:940`    | money keyword        | `.priority-card.arrears-check,`                                                                                                                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:942`    | money keyword        | `.priority-card.deposit-check,`                                                                                                                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:943`    | money keyword        | `.priority-card.rent-check{grid-column:auto}`                                                                                                                                                                          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:987`    | money keyword        | `/* Session summary hierarchy: core handover anchors first, details second */`                                                                                                                                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1006`   | money keyword        | `.handover-anchors{`                                                                                                                                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1012`   | money keyword        | `.handover-details{`                                                                                                                                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1017`   | money keyword        | `.handover-anchors .kpi-card{`                                                                                                                                                                                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1022`   | money keyword        | `.handover-anchors .kpi-card:nth-child(1){`                                                                                                                                                                            | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1027`   | money keyword        | `.handover-anchors .kpi-card:nth-child(2){`                                                                                                                                                                            | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1032`   | money keyword        | `.handover-anchors .kpi-card:nth-child(3){`                                                                                                                                                                            | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1037`   | money keyword        | `.handover-anchors .kpi-card span{`                                                                                                                                                                                    | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1040`   | money keyword        | `.handover-anchors .kpi-card b{`                                                                                                                                                                                       | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1045`   | money keyword        | `.handover-anchors .kpi-card:nth-child(1) b{color:#04964d!important}`                                                                                                                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1046`   | money keyword        | `.handover-anchors .kpi-card:nth-child(2) b{color:#1769e0!important}`                                                                                                                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1047`   | money keyword        | `.handover-anchors .kpi-card:nth-child(3) b{color:#d97706!important}`                                                                                                                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1048`   | money keyword        | `.handover-details .kpi-card{`                                                                                                                                                                                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1053`   | money keyword        | `.handover-details .kpi-card b{`                                                                                                                                                                                       | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1057`   | money keyword        | `.handover-anchors{grid-template-columns:1fr}`                                                                                                                                                                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1058`   | money keyword        | `.handover-details{grid-template-columns:repeat(2,minmax(0,1fr))}`                                                                                                                                                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1061`   | money keyword        | `.handover-details{grid-template-columns:1fr}`                                                                                                                                                                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1068`   | money keyword        | `.priority-card.arrears-check,`                                                                                                                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1070`   | money keyword        | `.priority-card.deposit-check,`                                                                                                                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1071`   | money keyword        | `.priority-card.rent-check{`                                                                                                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1392`   | money keyword        | `.handover-anchors{`                                                                                                                                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1397`   | money keyword        | `.handover-anchors .kpi-card{`                                                                                                                                                                                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1402`   | money keyword        | `.handover-anchors .kpi-card span,`                                                                                                                                                                                    | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1403`   | money keyword        | `.handover-details .kpi-card span{`                                                                                                                                                                                    | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1407`   | money keyword        | `.handover-anchors .kpi-card small,`                                                                                                                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1408`   | money keyword        | `.handover-details .kpi-card small{`                                                                                                                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1412`   | money keyword        | `.handover-anchors .kpi-card b{`                                                                                                                                                                                       | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1418`   | money keyword        | `.handover-details{`                                                                                                                                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1422`   | money keyword        | `.handover-details .kpi-card,`                                                                                                                                                                                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1428`   | money keyword        | `.handover-details .kpi-card b,`                                                                                                                                                                                       | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1508`   | money keyword        | `.priority-card.arrears-check,`                                                                                                                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1510`   | money keyword        | `.priority-card.deposit-check,`                                                                                                                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1511`   | money keyword        | `.priority-card.rent-check{`                                                                                                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1518`   | money keyword        | `.priority-card.arrears-check,`                                                                                                                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1717`   | schema floating type | `<div class="field"><label>员工密码 PIN</label><input id="loginPin" type="password" inputmode="numeric" placeholder="例如 8888" autocomplete="current-password"></div>`                                                | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v3.html:1730`   | money keyword        | `<button class="tab" data-view="arrears">跟进<span class="en">FOLLOW-UP</span></button>`                                                                                                                               | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1748`   | money keyword        | `<button class="event-chip" data-type="R"><span>收租</span><small>RENT</small></button>`                                                                                                                               | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1749`   | money keyword        | `<button class="event-chip" data-type="AP"><span>还欠款</span><small>ARREARS PAYMENT</small></button>`                                                                                                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1750`   | money keyword        | `<button class="event-chip" data-type="D"><span>收押金</span><small>DEPOSIT IN</small></button>`                                                                                                                       | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1751`   | money keyword        | `<button class="event-chip" data-type="DR"><span>退押金</span><small>DEPOSIT OUT</small></button>`                                                                                                                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1754`   | money keyword        | `<button class="event-chip" data-type="TF"><span>换床</span><small>BED TRANSFER</small></button>`                                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1763`   | money keyword        | `<div class="field" id="selectedEventWrap"><label>已选事件<span class="label-en">SELECTED EVENT</span></label><div id="selectedEventDisplay" class="readonly-pill">收租 RENT</div></div>`                              | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1780`   | money keyword        | `<div class="field"><label>付款方式 PY</label><select id="payType" class="hidden"><option value="C">现金 C</option><option value="B">银行 B</option></select><div class="pay-toggle"><button `                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1787`   | schema floating type | `<div class="field"><label>实收/支出金额 AMT</label><input id="amount" inputmode="decimal" placeholder="0.00"></div>`                                                                                                  | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v3.html:1788`   | schema floating type | `<div class="field"><label>系统月租 LP（老板端配置）</label><input id="listPrice" inputmode="decimal" placeholder="系统自动读取" readonly></div>`                                                                      | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v3.html:1801`   | schema floating type | `<div class="field"><label>账期天数 PD</label><input id="periodDays" inputmode="numeric" readonly></div>`                                                                                                              | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v3.html:1802`   | schema floating type | `<div class="field"><label>账期应收 period_due（系统计算）</label><input id="periodDue" inputmode="decimal" placeholder="系统计算" readonly></div>`                                                                    | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v3.html:1803`   | schema floating type | `<div class="field"><label>本条应收 DUE（系统计算）</label><input id="due" inputmode="decimal" placeholder="系统计算" readonly></div>`                                                                                 | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v3.html:1817`   | schema floating type | `<div class="field"><label>未提前告知扣款 NO_NOTICE</label><input id="noNoticeDeduction" inputmode="decimal" placeholder="0"></div>`                                                                                   | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v3.html:1818`   | schema floating type | `<div class="field"><label>其他扣款 OTHER</label><input id="otherDeduction" inputmode="decimal" placeholder="0"></div>`                                                                                                | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v3.html:1819`   | schema floating type | `<div class="field"><label>押金扣款合计 deposit_deduction</label><input id="depositDeduction" inputmode="decimal" placeholder="系统合计" readonly></div>`                                                              | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v3.html:1825`   | money keyword        | `<div class="field"><label>换床费处理</label><select id="feePaid"><option value="Y">收取 50 AED</option><option value="N">免收，必须填原因</option></select></div>`                                                    | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1833`   | schema floating type | `<div class="field"><label>系统计入 paid</label><input id="paid" inputmode="decimal" placeholder="系统自动计算" readonly></div>`                                                                                       | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v3.html:1839`   | money keyword        | `<div class="arrear-plan hidden" id="arrearPlan">`                                                                                                                                                                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1841`   | money keyword        | `<div>尾款任务<small>TAIL BALANCE TASK</small></div>`                                                                                                                                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1842`   | money keyword        | `<div class="plan-amount"><span id="arrearAmountText">0.00</span> AED</div>`                                                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1844`   | money keyword        | `<div class="amount-hint" id="amountFitHint"></div>`                                                                                                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1846`   | schema floating type | `<div class="field"><label>尾款金额 arrear_amount</label><input id="arrearAmount" inputmode="decimal" readonly></div>`                                                                                                 | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee-v3.html:1848`   | money keyword        | `<div class="field"><label>处理方式 arrear_handling</label><select id="arrearHandling"><option value="ARREAR">生成尾款任务 TAIL BALANCE</option><option value="FIX">返回修改 ENTRY ERROR</option><o`                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1868`   | money keyword        | `<div class="row" style="flex:0 0 auto"><button class="btn" id="btnPreviewSession">预览<span class="label-en">PREVIEW</span></button><button class="btn primary" id="btnExportSession">`                               | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1874`   | money keyword        | `<section id="view-arrears" class="hidden"><div class="card"><div class="head"><div><div class="title">收租跟进</div><div class="small">FOLLOW-UP · 通通锁过期 + 历史欠款</div></div><button class`                    | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1891`   | JS number operation  | `const num=v=>Number(String(v\|\|'').replace(/,/g,''));`                                                                                                                                                               | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:1892`   | JS number operation  | `const fmtMoney=n=>(Number(n)\|\|0).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});`                                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2046`   | JS number operation  | `const v=Number(state.rentConfig[key]);`                                                                                                                                                                               | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2049`   | JS number operation  | `function addMonths(dateStr,n){const d=new Date(dateStr+'T00:00:00');const day=d.getDate();d.setMonth(d.getMonth()+Number(n\|\|0));if(d.getDate()!==day)d.setDate(0);return `${d.getFul`                               | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2050`   | JS number operation  | `function addDays(dateStr,n){const d=new Date(dateStr+'T00:00:00');d.setDate(d.getDate()+Number(n\|\|0));return `${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`}`                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2051`   | JS number operation  | `function daysBetween(a,b){if(!a\|\|!b)return 0;return Math.max(0,Math.round((new Date(b+'T00:00:00')-new Date(a+'T00:00:00'))/86400000))}`                                                                            | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2053`   | JS number operation  | `function findDateText(s){s=String(s\|\|'');const full=s.match(/(20\d{2})[-/.年](\d{1,2})[-/.月](\d{1,2})/);if(full)return `${full[1]}-${pad(full[2])}-${pad(full[3])}`;const md=s.match(`                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2054`   | JS number operation  | `function dateFromAny(v){if(!v)return '';if(typeof v==='number'\|\|/^\d{10,13}$/.test(String(v))){const n=Number(v);const d=new Date(n<1e12?n*1000:n);if(!isNaN(d))return `${d.getFullY`                               | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2063`   | JS number operation  | `const m=Number(c[1]),d=Number(c[2]);`                                                                                                                                                                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2074`   | money keyword        | `let deposit=depositFromRemark(normalized);`                                                                                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2075`   | money keyword        | `if(!deposit&&bed&&tokens[1]&&/^(?:D)?\d{1,5}(?:\.\d{1,2})?$/i.test(tokens[1]))deposit=tokens[1].replace(/^D/i,'');`                                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2077`   | money keyword        | `if((!bed\|\|deposit===''\|\|!start_md)&&tokens.length>=3){`                                                                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2082`   | money keyword        | `if(b&&d!==''&&md){bed=bed\|\|b;deposit=deposit\|\|d;start_md=start_md\|\|md;break}`                                                                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2087`   | money keyword        | `if(deposit==='')missing.push('押金');`                                                                                                                                                                                | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2089`   | money keyword        | `return {bed,deposit,start_md,ok:missing.length===0,missing,raw:normalized};`                                                                                                                                          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2138`   | money keyword        | `const deposit=anchors.deposit\|\|depositFromRemark(remark)\|\|depositFromRemark(cardText)\|\|'';`                                                                                                                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2147`   | money keyword        | `return {bed,remark_bed:anchors.bed,room,tenant,tenant_card_id:String(cid),deposit,start,start_md:anchors.start_md,end,remark,cardText,vacant,staff_bed,followup_class:staff_bed?'sta`                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2163`   | money keyword        | `const rent=rentForBed(bed);`                                                                                                                                                                                          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2164`   | money keyword        | `$('listPrice').value=rent?fmtMoney(rent):'';`                                                                                                                                                                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2165`   | money keyword        | `if(c.deposit&&$('entryType').value==='D'&&!$('amount').value)$('amount').value=fmtMoney(c.deposit);`                                                                                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2173`   | money keyword        | `return state.depositBalance!=null?num(state.depositBalance):num(state.current?.deposit);`                                                                                                                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2177`   | money keyword        | `const r=await apiFetch('/api/employee/deposit?cid='+encodeURIComponent(cid));`                                                                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2179`   | money keyword        | `state.depositBalance=data&&data.success?num(data.balance):null;`                                                                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2187`   | money keyword        | `['匹配床位','MATCHED BED',c?.bed\|\|$('bed').value\|\|'未抓取',''],['CID','TTLOCK CID',$('tenantCardId')?.value\|\|c?.tenant_card_id\|\|'未抓取',''],['租客','TENANT',$('tenantName')?.value\|\|c?.tenant\|`          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2198`   | money keyword        | `const open=state.tasks.filter(t=>!['PAID','CLEARED','CLOSED','VOID','已结清','结清','作废','WRITTEN_OFF','WAIVED'].includes(String(t.close_status\|\|''))&&taskRemain(t)>0);`                                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2199`   | money keyword        | `sel.innerHTML='<option value="">请选择要偿还的欠款</option>'+open.map(t=>`<option value="${esc(t.task_id)}">#${esc(t.bed\|\|'')} ${esc(t.tenant_name\|\|'-')} · ${fmtMoney(taskRemain(t))} AED · `                    | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2206`   | money keyword        | `const due=num($('periodDue').value)\|\|num($('due').value);`                                                                                                                                                          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2207`   | money keyword        | `const amt=num($('amount').value);`                                                                                                                                                                                    | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2208`   | money keyword        | `if(!due\|\|!amt\|\|amt>=due)return 0;`                                                                                                                                                                                | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2209`   | money keyword        | `return Math.max(0,due-Math.min(amt,due));`                                                                                                                                                                            | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2212`   | money keyword        | `const amt=num($('amount').value);`                                                                                                                                                                                    | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2213`   | money keyword        | `const due=num($('periodDue').value)\|\|num($('due').value);`                                                                                                                                                          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2214`   | money keyword        | `if(!amt\|\|!due)return '录入实收金额后，系统会判断这笔钱是完整账期、15天、日租，还是只能作为部分收款生成尾款。';`                                                                                                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2215`   | money keyword        | `if(Math.abs(amt-400)<0.01&&$('cycle').value!=='15D')return '实收金额刚好等于 15 天租金 400 AED。如果业务实际只收 15 天，应回到第六步选择 15天；如果本次仍是整月账期，则必须生成尾款任务。';`                          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2217`   | money keyword        | `if(Number.isInteger(days)&&days>0&&amt!==due&&$('cycle').value!=='CUST')return `实收金额等于 ${days} 天日租。如果业务实际是日租，应回到第六步选择自定义 ${days} 天；如果本次仍按当前账期核算，则必须生成尾款任务。`;` | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2218`   | money keyword        | `if(amt<due)return '实收金额小于账期应收，且不自动改变账期性质。系统会把差额作为尾款任务，后续在“还欠款”中回收。';`                                                                                                    | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2219`   | money keyword        | `if(amt>due)return '实收金额大于应收，必须明确超收去向，不能自动计入本期收入。';`                                                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2227`   | money keyword        | `if(!$('arrearHandling').value)$('arrearHandling').value='ARREAR';`                                                                                                                                                    | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2242`   | money keyword        | `return state.tasks.filter(t=>!['PAID','CLEARED','CLOSED','VOID','已结清','结清','作废','WRITTEN_OFF','WAIVED'].includes(String(t.close_status\|\|''))&&taskRemain(t)>0&&((bed&&String(t.bed\|\|`                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2260`   | money keyword        | `$('apTaskInfo').innerHTML=t?`关联欠款：<strong>#${esc(t.bed\|\|'')}</strong> ${esc(t.tenant_name\|\|'-')}，剩余 <strong>${fmtMoney(taskRemain(t))} AED</strong>，账期 ${esc(t.original_period_start`                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2271`   | money keyword        | `$('amount').value=fmtMoney(remain);`                                                                                                                                                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2272`   | money keyword        | `$('due').value=fmtMoney(remain);`                                                                                                                                                                                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2273`   | money keyword        | `$('paid').value=fmtMoney(remain);`                                                                                                                                                                                    | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2284`   | money keyword        | `if(e.type==='TF')return num(e.amount)>0?'in':'none';`                                                                                                                                                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2290`   | money keyword        | `const cashIn=rows.filter(e=>e.pay_type==='C'&&entryDirection(e)==='in').reduce((s,e)=>s+num(e.amount),0);`                                                                                                            | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2291`   | money keyword        | `const bankIn=rows.filter(e=>e.pay_type==='B'&&entryDirection(e)==='in').reduce((s,e)=>s+num(e.amount),0);`                                                                                                            | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2292`   | money keyword        | `const bankCount=rows.filter(e=>e.pay_type==='B'&&entryDirection(e)==='in'&&num(e.amount)>0).length;`                                                                                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2293`   | money keyword        | `const rentIn=rows.filter(e=>e.type==='R').reduce((s,e)=>s+num(e.paid\|\|e.amount),0);`                                                                                                                                | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2294`   | money keyword        | `const depositIn=rows.filter(e=>e.type==='D').reduce((s,e)=>s+num(e.amount),0);`                                                                                                                                       | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2295`   | money keyword        | `const arrearsIn=rows.filter(e=>e.type==='AP').reduce((s,e)=>s+num(e.amount),0);`                                                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2296`   | money keyword        | `const transferFee=rows.filter(e=>e.type==='TF'\|\|e.type==='TFF').reduce((s,e)=>s+num(e.amount),0);`                                                                                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2297`   | money keyword        | `const depRefund=rows.filter(e=>e.type==='DR').reduce((s,e)=>s+num(e.amount),0);`                                                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2298`   | money keyword        | `const expenses=rows.filter(e=>e.type==='E').reduce((s,e)=>s+num(e.amount),0);`                                                                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2299`   | money keyword        | `const cashOut=rows.filter(e=>e.pay_type==='C'&&entryDirection(e)==='out').reduce((s,e)=>s+num(e.amount),0);`                                                                                                          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2300`   | money keyword        | `const bankOut=rows.filter(e=>e.pay_type==='B'&&entryDirection(e)==='out').reduce((s,e)=>s+num(e.amount),0);`                                                                                                          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2311`   | money keyword        | `['租金收入','RENT',s.rentIn,'','本期租金入账'],`                                                                                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2312`   | money keyword        | `['押金收取','DEPOSIT IN',s.depositIn,'blue','押金是负债'],`                                                                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2313`   | money keyword        | `['欠款回收','ARREARS',s.arrearsIn,'orange','历史应收回款'],`                                                                                                                                                          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2314`   | money keyword        | `['换床费','TRANSFER FEE',s.transferFee,'','服务费收入'],`                                                                                                                                                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2315`   | money keyword        | `['押金退款','DEP. REFUND',s.depRefund,'orange','押金流出'],`                                                                                                                                                          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2317`   | money keyword        | `['现金应交','CASH DUE',s.cashBalance,'blue','员工本票应交现金']`                                                                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2319`   | money keyword        | `$('sessionKpis').innerHTML=cards.map(([cn,en,v,cls,note])=>`<div class="kpi-card"><span>${cn}</span><small>${en}</small><b class="${cls}">${fmtDisplayMoney(v)}</b><small>${note}</s`                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2348`   | money keyword        | `return `<div class="preview-entry"><div><b>${rows.length-i}. ${subject}</b> ${status}<div class="small">${esc(detail)}</div><div class="small">EID ${esc(e.id)} · ${esc(e.created_at`                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2360`   | money keyword        | `['amount','due','paid','periodStart','periodEnd','periodDays','periodDue','customReason','remark','arrearAmount','arrearPromiseDate','arrearReasonDetail','depositDeduction','deposi`                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2361`   | money keyword        | `$('arrearHandling').value='ARREAR';`                                                                                                                                                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2369`   | money keyword        | `const map={R:'收租 RENT',AP:'还欠款 ARREARS PAYMENT',D:'收押金 DEPOSIT IN',DR:'退押金 DEPOSIT OUT',CO:'退房 CHECKOUT',E:'支出 EXPENSE',TF:'换床 BED TRANSFER'};`                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2381`   | money keyword        | `const type=$('entryType').value, amt=num($('amount').value), due=num($('due').value), paid=num($('paid').value);`                                                                                                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2382`   | money keyword        | `return (due>0&&amt!==due)\|\|($('entryClr').value!=='Y')\|\|['DISCOUNT','OTHER'].includes($('reasonCode').value)\|\|$('feePaid').value==='N'\|\|type==='CO';`                                                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2392`   | money keyword        | `const total=overDed+noNotice+other;`                                                                                                                                                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2393`   | money keyword        | `return {overDays,earlyDays,overDed,noNotice,other,total};`                                                                                                                                                            | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2404`   | money keyword        | `const diff=Math.max(0,num($('due').value)-num($('paid').value\|\|$('amount').value));`                                                                                                                                | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2410`   | money keyword        | `['金额',`${fmtMoney(num($('amount').value))} AED`],`                                                                                                                                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2411`   | money keyword        | `['应收/计入',`${fmtMoney(num($('due').value))} / ${fmtMoney(num($('paid').value\|\|$('amount').value))}`],`                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2414`   | money keyword        | `['差额',diff?`${fmtMoney(diff)} AED`:'无'],`                                                                                                                                                                          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2415`   | money keyword        | `['押金余额',depositHeld()?`${fmtMoney(depositHeld())} AED`:'无'],`                                                                                                                                                    | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2420`   | money keyword        | `items.push(['尾款任务',`${fmtMoney(tail)} AED / ${$('arrearPromiseDate').value\|\|'未填日期'}`]);`                                                                                                                    | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2423`   | money keyword        | `if(type==='R'&&num($('amount').value)>num($('due').value))items.push(['超收处理',$('excessTo').value\|\|'未选择']);`                                                                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2424`   | money keyword        | `if(type==='CO'){const co=checkoutCalc();items.push(['退房扣款',`${fmtMoney(co.total)} / 超期${co.overDays}天 / 提前${co.earlyDays}天`]);}`                                                                            | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2430`   | money keyword        | `const rent=rentForBed();`                                                                                                                                                                                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2431`   | money keyword        | `if(type==='R')$('listPrice').value=rent?fmtMoney(rent):'';`                                                                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2437`   | money keyword        | `visible(['excessWrap'],type==='R'&&num($('amount').value)>num($('due').value));`                                                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2451`   | money keyword        | `if(type==='TF'&&$('feePaid').value==='Y'){$('amount').value='50.00';$('due').value='50.00';$('paid').value='50.00';$('periodDue').value='';}`                                                                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2452`   | money keyword        | `if(type==='TF'&&$('feePaid').value==='N'){$('amount').value='0.00';$('due').value='0.00';$('paid').value='0.00';$('periodDue').value='';}`                                                                            | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2453`   | money keyword        | `if(type==='CO'){if(!$('checkoutDate').value)$('checkoutDate').value=today();if(!$('amount').value)$('amount').value='0.00';if(!$('due').value)$('due').value='0.00';if(!$('paid').va`                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2454`   | money keyword        | `if(type==='TFF'){if(!$('amount').value\|\|num($('amount').value)===0)$('amount').value='50.00';$('due').value='50.00';$('periodDue').value='50.00';$('cycle').value='1M';}`                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2459`   | money keyword        | `const lp=rent;`                                                                                                                                                                                                       | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2467`   | money keyword        | `if(type==='R'\|\|type==='TFF')$('due').value=$('periodDue').value\|\|'';`                                                                                                                                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2468`   | money keyword        | `else if(hasPeriod&&!$('due').value)$('due').value=$('periodDue').value\|\|$('amount').value;`                                                                                                                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2469`   | money keyword        | `const amt=num($('amount').value), due=num($('due').value);`                                                                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2470`   | money keyword        | `if(['R','AP','TFF'].includes(type))$('paid').value=fmtMoney(Math.min(amt,due\|\|amt));`                                                                                                                               | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2471`   | money keyword        | `else if(['TF','CO'].includes(type)&&amt===0)$('paid').value='0.00';`                                                                                                                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2472`   | money keyword        | `else if(amt)$('paid').value=fmtMoney(amt);`                                                                                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2473`   | money keyword        | `$('entryClr').value=due&&amt<due?'N':'Y';`                                                                                                                                                                            | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2474`   | money keyword        | `if(type==='R'&&due>0&&amt>0&&amt<due&&!$('reasonCode').value)$('reasonCode').value='SHORT_PAID';`                                                                                                                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2475`   | money keyword        | `if(type==='DR'&&depositHeld()>0&&!$('amount').value)$('amount').value=fmtMoney(depositHeld());`                                                                                                                       | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2476`   | money keyword        | `if(type==='CO'){const co=checkoutCalc();$('overdayDeduction').value=fmtMoney(co.overDed);$('depositDeduction').value=fmtMoney(co.total);$('depositReturnAmount').value=fmtMoney(Math`                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2479`   | money keyword        | `visible(['excessWrap'],type==='R'&&num($('amount').value)>num($('due').value));`                                                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2487`   | money keyword        | `const amt=num($('amount').value), due=num($('due').value);`                                                                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2493`   | money keyword        | `if(['D','DR','CO'].includes(type)&&state.current&&!state.current.deposit)errors.push('押金相关事件必须从通通锁备注解析到押金金额，不能靠员工猜。');`                                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2494`   | money keyword        | `if(type==='D'&&state.current?.deposit&&amt>0&&Math.abs(amt-num(state.current.deposit))>0.01&&!$('remark').value.trim())errors.push('收押金金额与通通锁备注押金不一致，必须填写原因。');`                              | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2496`   | money keyword        | `if(type==='R'&&$('cycle').value==='CUST'&&num($('periodDays').value)<=0)errors.push('自定义天数必须填写大于 0 的天数，系统按每天 40 AED 计算。');`                                                                    | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2503`   | money keyword        | `if(type==='R'&&samePeriodTask)errors.push(`当前账期已有未清尾款任务 ${fmtMoney(taskRemain(samePeriodTask))} AED，请改用“还欠款 AP”处理，不能重复按收租录入。`);`                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2505`   | money keyword        | `if($('arrearHandling').value!=='ARREAR')errors.push('实收小于账期应收时，只能生成尾款任务；如金额或账期录错，请先返回修改，不能提交。');`                                                                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2515`   | money keyword        | `if(type==='R'&&due>0&&amt>due&&!$('excessTo').value)errors.push('收租超收必须选择处理方式：找零已退还，或转老板处理。');`                                                                                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2517`   | money keyword        | `if(type==='TF'&&$('feePaid').value==='Y'&&amt!==50)errors.push('换床收取费用时，系统固定按 50 AED 计入，员工不能手动改金额。');`                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2523`   | money keyword        | `if(amt>due&&due>0&&type==='R')warns.push('AMT>DUE：系统不会自动把超收当作本期收入，必须明确超收去向。');`                                                                                                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2530`   | money keyword        | `const type=$('entryType').value, amt=num($('amount').value), due=num($('due').value), paid=num($('paid').value)\|\|Math.min(amt,due\|\|amt);`                                                                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2542`   | money keyword        | `id:uid('E'),entry_id:null,type,reason_code:reasonCode,cat:pay==='B'?'bank':(type==='E'\|\|type==='DR'?'expense':'cash'),pay_type:pay,bank_ref:$('bankRef').value.trim(),`                                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2545`   | money keyword        | `amount:amt,due,paid,deficit:Math.max(0,due-paid),entry_clr:$('entryClr').value,clr:$('entryClr').value,excess:Math.max(0,amt-due),excess_to:$('excessTo').value,`                                                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2547`   | money keyword        | `period_day_count:num($('periodDays').value),period_due:num($('periodDue').value)\|\|due,custom_reason:$('customReason').value.trim()\|\|tailReason,`                                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2548`   | money keyword        | `checkout_date:$('checkoutDate').value,deposit_held:depositHeld(),deposit_return:type==='CO'?depositReturnStatus(depositHeld(),co.total):'',deposit_amt:type==='CO'?Math.max(0,deposi`                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2561`   | money keyword        | `const tail=Math.max(0,num(e.period_due)-num(e.paid));`                                                                                                                                                                | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2563`   | money keyword        | `return `#${e.room} ${name} R ${e.tag} ${fmtMoney(e.amount)} AED ${e.entry_clr==='Y'?'CLR':'short paid'}${tailText}${num(e.excess)>0?' EXCESS'+fmtMoney(e.excess)+' '+(e.excess_to\|\|'`                               | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2565`   | money keyword        | `if(e.type==='D')return `#${e.room} ${name} D ${e.tag} ${fmtMoney(e.amount)} AED\n card ${cid} EID ${e.id}`;`                                                                                                          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2566`   | money keyword        | `if(e.type==='DR')return `#${e.room} ${name} DR ${fmtMoney(e.amount)} AED ${e.pay_type}\n card ${cid} EID ${e.id}`;`                                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2567`   | money keyword        | `if(e.type==='CO')return `#${e.room} ${name} left ${e.checkout_date\|\|'-'} CO\n card ${cid}\n deposit held ${fmtMoney(e.deposit_held)} deducted ${fmtMoney(e.deposit_deduction)} ret`                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2568`   | money keyword        | `if(e.type==='AP')return `#${e.room} ${name} AP ${fmtMoney(e.amount)} AED ${e.pay_type} closes TASK ${e.linked_task_id\|\|'-'}\n  card ${cid}${period} EID ${e.id}`;`                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2570`   | money keyword        | `if(e.type==='TFF')return `#${e.room} ${name} TFF ${fmtMoney(e.amount)} AED ${e.pay_type}\n card ${cid} EID ${e.id}`;`                                                                                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2571`   | money keyword        | `return `#${e.room\|\|e.expense_category} ${fmtMoney(e.amount)} AED ${e.pay_type} ${e.expense_desc\|\|e.note\|\|''}\n EID ${e.id}`;`                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2596`   | money keyword        | `function resetForm(){['bed','bedTo','amount','due','paid','tenantName','tenantCardId','periodStart','periodEnd','periodDays','periodDue','listPrice','customReason','bankRef','check`                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2622`   | money keyword        | `return state.tasks.filter(t=>!['PAID','CLEARED','CLOSED','VOID','已结清','结清','作废','WRITTEN_OFF','WAIVED'].includes(String(t.close_status\|\|''))&&taskRemain(t)>0);`                                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2630`   | money keyword        | `const amount=rentForBed(c.bed);`                                                                                                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2640`   | money keyword        | `deposit:c.deposit\|\|'',`                                                                                                                                                                                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2644`   | money keyword        | `amount,`                                                                                                                                                                                                              | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2681`   | money keyword        | `deposit:card?.deposit\|\|'',`                                                                                                                                                                                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2685`   | money keyword        | `amount:taskRemain(t),`                                                                                                                                                                                                | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2703`   | JS number operation  | `items.sort((a,b)=>(Number(b.force)-Number(a.force))\|\|(b.overdue_days-a.overdue_days)\|\|(b.amount-a.amount));`                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2719`   | money keyword        | `const rentNote=item.amount>0?`${fmtMoney(item.amount)} AED`:'未配置月租';`                                                                                                                                            | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2740`   | money keyword        | `备注锚点 REMARK: ${esc(item.lock_remark\|\|item.tenant_name\|\|'-')} · CID: ${esc(item.tenant_card_id\|\|'-')} · 押金 DEPOSIT: ${esc(item.deposit?fmtMoney(item.deposit):'-')}<br>`                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2741`   | money keyword        | `日期锚点 PERIOD: ${esc(item.period_start\|\|'-')} → ${esc(item.period_end\|\|'-')} · 到期 DUE: ${esc(item.due_date\|\|'-')}<br>`                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2751`   | money keyword        | `<button class="mini-btn" data-fill-rent="${esc(item.task_id)}">去收租</button>`                                                                                                                                       | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2758`   | money keyword        | `const due=items.filter(x=>x.source==='ttlock');`                                                                                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2760`   | money keyword        | `const total=items.reduce((s,x)=>s+num(x.amount),0);`                                                                                                                                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2763`   | money keyword        | `<div class="followup-metric warn"><span>通通锁过期</span><small>TTLOCK OVERDUE</small><b>${due.length}</b></div>`                                                                                                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2764`   | money keyword        | `<div class="followup-metric"><span>历史欠款</span><small>ARREARS</small><b>${history.length}</b></div>`                                                                                                               | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2765`   | money keyword        | `<div class="followup-metric"><span>应收提醒</span><small>AMOUNT</small><b>${fmtMoney(total)}</b></div>`                                                                                                               | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2776`   | money keyword        | `document.querySelectorAll('[data-fill-rent]').forEach(btn=>btn.onclick=()=>prefillRentFromFollowup(btn.dataset.fillRent));`                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2802`   | money keyword        | `if(num(item.amount)<=0){`                                                                                                                                                                                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2821`   | money keyword        | `promise_amount:num(item.amount),`                                                                                                                                                                                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2830`   | money keyword        | `arrear_amount:num(item.amount),`                                                                                                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2861`   | money keyword        | `$('amount').value='';`                                                                                                                                                                                                | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2872`   | money keyword        | `'Period rule: 1M same-date anchor; 15D inclusive end date; CUST = days * 40 AED',`                                                                                                                                    | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2877`   | money keyword        | `section('CASH RECEIVED',drafts.filter(e=>e.pay_type==='C'&&['R','D','TF','TFF'].includes(e.type)&&num(e.amount)>0));`                                                                                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2878`   | money keyword        | `section('BANK RECEIVED',drafts.filter(e=>e.pay_type==='B'&&['R','D','TF','TFF'].includes(e.type)&&num(e.amount)>0));`                                                                                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2879`   | money keyword        | `section('ARREAR REPAID',drafts.filter(e=>e.type==='AP'));`                                                                                                                                                            | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2881`   | money keyword        | `section('DEPOSIT RETURN',drafts.filter(e=>e.type==='DR'));`                                                                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2883`   | money keyword        | `section('TRANSFER',drafts.filter(e=>e.type==='TF'));`                                                                                                                                                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2884`   | money keyword        | `const opened=drafts.filter(e=>e.type==='R'&&e.entry_clr!=='Y'&&num(e.period_due)>num(e.paid));`                                                                                                                       | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2886`   | money keyword        | `lines.push('', '==== ARREARS OPENED ====');`                                                                                                                                                                          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2887`   | money keyword        | `opened.forEach(e=>lines.push(`TASK auto #${e.room} ${e.tenant_name\|\|'-'} ${e.tenant_card_id\|\|'-'} owes ${fmtMoney(num(e.period_due)-num(e.paid))} ${e.reason_code\|\|'SHORT_PAID'} `                              | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2889`   | money keyword        | `const cashIn=drafts.filter(e=>e.pay_type==='C'&&!['E','DR'].includes(e.type)).reduce((s,e)=>s+num(e.amount),0);`                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2890`   | money keyword        | `const bankIn=drafts.filter(e=>e.pay_type==='B'&&!['E','DR'].includes(e.type)).reduce((s,e)=>s+num(e.amount),0);`                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2891`   | money keyword        | `const cashOut=drafts.filter(e=>e.pay_type==='C'&&['E','DR'].includes(e.type)).reduce((s,e)=>s+num(e.amount),0);`                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2892`   | money keyword        | `const bankOut=drafts.filter(e=>e.pay_type==='B'&&['E','DR'].includes(e.type)).reduce((s,e)=>s+num(e.amount),0);`                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2893`   | money keyword        | `const rentIn=drafts.filter(e=>e.type==='R').reduce((s,e)=>s+num(e.paid\|\|e.amount),0);`                                                                                                                              | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2894`   | money keyword        | `const depositIn=drafts.filter(e=>e.type==='D').reduce((s,e)=>s+num(e.amount),0);`                                                                                                                                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2895`   | money keyword        | `const arrearsIn=drafts.filter(e=>e.type==='AP').reduce((s,e)=>s+num(e.amount),0);`                                                                                                                                    | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2896`   | money keyword        | `const feeIn=drafts.filter(e=>e.type==='TF'\|\|e.type==='TFF').reduce((s,e)=>s+num(e.amount),0);`                                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2897`   | money keyword        | `const bankCount=drafts.filter(e=>e.pay_type==='B'&&!['E','DR'].includes(e.type)&&num(e.amount)>0).length;`                                                                                                            | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2898`   | money keyword        | `lines.push('','==== HANDOVER ANCHORS ====',`Cash handover: ${fmtMoney(cashIn-cashOut)}`,`Bank transfer total: ${fmtMoney(bankIn)} (${bankCount} txns)`,`Gross received: ${fmtMoney(c`                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2899`   | money keyword        | `lines.push('','==== SUMMARY ====',`Rent income: ${fmtMoney(rentIn)}`,`Deposit received: ${fmtMoney(depositIn)}`,`Arrears repaid: ${fmtMoney(arrearsIn)}`,`Transfer fee: ${fmtMoney(f`                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2908`   | money keyword        | `const tail=Math.max(0,num(e.period_due)-num(e.paid));`                                                                                                                                                                | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2914`   | money keyword        | `<div class="preview-detail-top"><b>${i+1}. ${esc(eventLabel(e.type))} · ${esc(subject)}</b><b class="money">${fmtMoney(e.amount)} AED</b></div>`                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2916`   | money keyword        | `${previewField('付款方式 PAYMENT',pay)}`                                                                                                                                                                              | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2919`   | money keyword        | `${previewField('应收/计入 DUE/PAID',`${fmtMoney(e.due)} / ${fmtMoney(e.paid)}`)}`                                                                                                                                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2920`   | money keyword        | `${previewField('尾款 TAIL',tail>0?`${fmtMoney(tail)} AED · ${e.arrear_promise_date\|\|'-'}`:'无')}`                                                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2936`   | money keyword        | `<div class="preview-anchor"><span>现金结余 CASH HANDOVER</span><b>${fmtMoney(s.cashBalance)}</b></div>`                                                                                                               | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:2937`   | money keyword        | `<div class="preview-anchor"><span>银行转账 BANK TRANSFER</span><b>${fmtMoney(s.bankIn)} · ${s.bankCount}笔</b></div>`                                                                                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3009`   | money keyword        | `document.querySelectorAll('.tab').forEach(btn=>btn.onclick=()=>{document.querySelectorAll('.tab').forEach(b=>b.classList.remove('active'));btn.classList.add('active');['entry','arr`                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3012`   | money keyword        | `['entryType','cycle','payType','amount','reasonCode','feePaid','customReason','tenantName','bankRef','periodDays','depositDeduction','checkoutDate','noNoticeDeduction','otherDeduct`                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3037`   | money keyword        | `['第四步：付款方式','STEP 4 · PAYMENT METHOD'],`                                                                                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3038`   | money keyword        | `['第五步：填写金额','STEP 5 · ENTER AMOUNT'],`                                                                                                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3044`   | money keyword        | `const eventNames={R:['收租','RENT'],AP:['还欠款','ARREARS PAYMENT'],D:['收押金','DEPOSIT IN'],DR:['退押金','DEPOSIT OUT'],CO:['退房','CHECKOUT'],E:['支出','EXPENSE'],TF:['换床','BED TRANSFER']};`                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3047`   | money keyword        | `['R','收租','RENT'],['D','收押金','DEPOSIT IN'],['DR','退押金','DEPOSIT OUT'],['CO','退房','CHECKOUT'],['AP','还欠款','ARREARS PAYMENT'],['E','支出','EXPENSE'],['TF','换床','BED TRANSFER']`                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3049`   | money keyword        | `$('payType').innerHTML='<option value="C">现金 CASH</option><option value="B">银行 BANK</option>';`                                                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3051`   | money keyword        | `label('payType','付款方式','PAYMENT METHOD');label('bankRef','银行参考','BANK REF');label('amount','实收/支出金额','AMOUNT');label('listPrice','系统月租','LIST PRICE');`                                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3053`   | money keyword        | `label('periodDue','账期应收','PERIOD DUE');label('due','本条应收','ENTRY DUE');label('linkedTaskId','选择欠款任务','ARREARS TASK');`                                                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3055`   | money keyword        | `label('otherDeduction','其他扣款','OTHER DEDUCTION');label('depositDeduction','押金扣款合计','DEPOSIT DEDUCTION');label('depositReturnAmount','应退押金','DEPOSIT REFUND');`                                          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3056`   | money keyword        | `label('dedNote','扣款说明','DEDUCTION NOTE');label('bedTo','换至床位','BED TO');label('feePaid','换床费处理','TRANSFER FEE');`                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3057`   | money keyword        | `label('expenseCategory','支出类别','EXPENSE CATEGORY');label('expenseDesc','支出说明','EXPENSE DESCRIPTION');label('paid','系统计入','SYSTEM PAID');label('entryClr','系统结清','SYSTEM CLEAR');`                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3058`   | money keyword        | `label('excessTo','超收处理','EXCESS HANDLING');label('arrearAmount','尾款金额','TAIL BALANCE');label('arrearPromiseDate','承诺支付日期','PROMISE DATE');label('arrearHandling','处理方式','HANDLING');`               | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3067`   | money keyword        | `const rent=rentForBed()?fmtMoney(rentForBed()):'未配置';`                                                                                                                                                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3076`   | money keyword        | ``<div class="ctx-panel"><div class="ctx-title">金额锚点<small>AMOUNT ANCHORS</small></div>${ctxRow('系统月租','LIST PRICE',rent,'money')}${ctxRow('卡片押金','CARD DEPOSIT',c?.deposit?fmtMoney(`                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3087`   | money keyword        | `['现金结余','CASH HANDOVER',s.cashBalance,'anchor','应面对面交接现金'],`                                                                                                                                              | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3088`   | money keyword        | `['银行转账总计','BANK TRANSFER',s.bankIn,'anchor bank',`${s.bankCount} 笔银行入账`],`                                                                                                                                 | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3089`   | money keyword        | `['收款总计','GROSS RECEIVED',s.grossReceived,'anchor total','现金总收入 + 银行总收入']`                                                                                                                               | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3092`   | money keyword        | `['租金收入','RENT',s.rentIn,'','本期租金入账'],`                                                                                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3093`   | money keyword        | `['押金收取','DEPOSIT IN',s.depositIn,'blue','押金是负债'],`                                                                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3094`   | money keyword        | `['欠款回收','ARREARS',s.arrearsIn,'orange','历史应收回款'],`                                                                                                                                                          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3095`   | money keyword        | `['换床费','TRANSFER FEE',s.transferFee,'','服务费收入'],`                                                                                                                                                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3096`   | money keyword        | `['押金退款','DEP. REFUND',s.depRefund,'orange','现金支出项'],`                                                                                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3100`   | money keyword        | `grid.innerHTML=`<div class="kpi-section-label">交接核心<small>HANDOVER CORE</small></div><div class="handover-anchors">${anchors.map(card).join('')}</div><div class="kpi-section-label"`                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3120`   | money keyword        | `['第四步：付款方式','STEP 4 · PAYMENT METHOD'],`                                                                                                                                                                      | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3121`   | money keyword        | `['第五步：填写金额','STEP 5 · ENTER AMOUNT'],`                                                                                                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3127`   | money keyword        | `const eventNames={R:['收租','RENT'],AP:['还欠款','ARREARS PAYMENT'],D:['收押金','DEPOSIT IN'],DR:['退押金','DEPOSIT OUT'],CO:['退房','CHECKOUT'],E:['支出','EXPENSE'],TF:['换床','BED TRANSFER']};`                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3130`   | money keyword        | `['R','收租','RENT'],['D','收押金','DEPOSIT IN'],['DR','退押金','DEPOSIT OUT'],['CO','退房','CHECKOUT'],['AP','还欠款','ARREARS PAYMENT'],['E','支出','EXPENSE'],['TF','换床','BED TRANSFER']`                         | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3132`   | money keyword        | `$('payType').innerHTML='<option value="C">现金 CASH</option><option value="B">银行 BANK</option>';`                                                                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3134`   | money keyword        | `label('payType','付款方式','PAYMENT METHOD');label('bankRef','银行参考','BANK REF');label('amount','实收/支出金额','AMOUNT');label('listPrice','系统月租','LIST PRICE');`                                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3136`   | money keyword        | `label('periodDue','账期应收','PERIOD DUE');label('due','本条应收','ENTRY DUE');label('linkedTaskId','选择欠款任务','ARREARS TASK');`                                                                                  | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3138`   | money keyword        | `label('otherDeduction','其他扣款','OTHER DEDUCTION');label('depositDeduction','押金扣款合计','DEPOSIT DEDUCTION');label('depositReturnAmount','应退押金','DEPOSIT REFUND');`                                          | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3139`   | money keyword        | `label('dedNote','扣款说明','DEDUCTION NOTE');label('bedTo','换至床位','BED TO');label('feePaid','换床费处理','TRANSFER FEE');`                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3140`   | money keyword        | `label('expenseCategory','支出类别','EXPENSE CATEGORY');label('expenseDesc','支出说明','EXPENSE DESCRIPTION');label('paid','系统计入','SYSTEM PAID');label('entryClr','系统结清','SYSTEM CLEAR');`                     | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3141`   | money keyword        | `label('excessTo','超收处理','EXCESS HANDLING');label('arrearAmount','尾款金额','TAIL BALANCE');label('arrearPromiseDate','承诺支付日期','PROMISE DATE');label('arrearHandling','处理方式','HANDLING');`               | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3155`   | money keyword        | `const rent=rentForBed()?fmtMoney(rentForBed()):'未配置';`                                                                                                                                                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3168`   | money keyword        | `const arrearText=open.length?`${open.length} 笔 / ${fmtMoney(arrearTotal)} AED`:'无未清欠款';`                                                                                                                        | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3169`   | money keyword        | `const depositText=c?.deposit?fmtMoney(c.deposit):'未抓取';`                                                                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3176`   | money keyword        | `<div class="priority-card arrears-check ${open.length?'warn':'strong'}"><span>历史欠款<small>ARREARS</small></span><b>${esc(arrearText)}</b><div class="sub">${open.length?'优先确认是否还旧账':'`                    | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3178`   | money keyword        | `<div class="priority-card deposit-check"><span>押金<small>DEPOSIT</small></span><b>${esc(depositText)}</b><div class="sub">台账押金 ${esc(ledgerText)}</div></div>`                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee-v3.html:3179`   | money keyword        | `<div class="priority-card rent-check"><span>系统月租<small>LIST PRICE</small></span><b>${esc(rent)}</b><div class="sub">老板端配置租金</div></div>`                                                                   | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee.html:74`        | money keyword        | `<button class="tab" data-view="arrears">欠款<span class="en-sub">ARREARS</span></button>`                                                                                                                             | Treat as display/input only; backend must parse and recompute authoritative integer fils. |
| Frontend | `deploy-worker/public/employee.html:94`        | schema floating type | `<div class="field"><label>实收金额 amount</label><input id="amount" inputmode="decimal" placeholder="400.00"></div>`                                                                                                  | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee.html:98`        | schema floating type | `<div class="field"><label>应收金额 due</label><input id="due" inputmode="decimal" placeholder="系统参考或手动填写"></div>`                                                                                            | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee.html:99`        | schema floating type | `<div class="field"><label>计入金额 paid</label><input id="paid" inputmode="decimal" placeholder="默认等于实收"></div>`                                                                                                | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee.html:107`       | schema floating type | `<div class="field hidden" id="customDaysWrap"><label>自定义天数</label><input id="customDays" inputmode="numeric" placeholder="例如 10"></div>`                                                                       | Migrate commercial money storage to INTEGER fils with legacy fallback and reconciliation. |
| Frontend | `deploy-worker/public/employee.html:121`       | money keyword        | `<section id="view-arrears" class="hidden">`                                                                                                                                                                           | Treat as display/input only; backend must parse and recompute authoritative integer fils. |

Report truncated to 350 of 2625 findings. Use `npm run audit:money` to regenerate counts.
