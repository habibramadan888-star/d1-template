# Next Prompt: Arrears Local Implementation

```text
进入 TASK ARREARS-LOCAL-IMPLEMENTATION-001。
目标：在本地实现 arrears follow-up API/UI/tests，使用 mock/local dry-run 数据。
禁止 production deploy。
禁止 production/staging/production-copy D1 write。
禁止执行 migration。
允许新增本地测试、API handler、UI wiring、node --test 回归。
必须输出 local-only verification result。
必须保持 PRODUCTION_NO_GO。
```
