# Backend Totals Shadow Result

Generated: 2026-05-24T07:39:15.984Z, Asia/Dubai

Scope: P0-003A static audit only. This report does not change dashboard totals, handover submission, Worker responses, database schema, or production configuration.

## Summary

| Metric                                          | Count |
| ----------------------------------------------- | ----: |
| Frontend submitted total references             |    36 |
| Numeric reduction / operation references        |   539 |
| Backend legacy submitted-total parse references |    11 |
| Backend recompute / shadow evidence references  |    24 |
| Total findings                                  |   610 |

## Findings

| Area              | File                                         | Kind                        | Evidence                                                                                                                                                                                            | Recommendation                                                                         |
| ----------------- | -------------------------------------------- | --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| Worker backend    | `deploy-worker/src/index.js:121`             | numeric-operation           | `return Array.from(bytes).map((b) => b.toString(16).padStart(2, "0")).join("");`                                                                                                                    | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:259`             | numeric-operation           | `const explicit = String(env.ALLOWED_ORIGINS \|\| "").split(",").map((v) => v.trim()).filter(Boolean);`                                                                                             | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:267`             | numeric-operation           | `].filter(Boolean));`                                                                                                                                                                               | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:309`             | numeric-operation           | `const connect = Array.from(new Set(["'self'", origin, apiOrigin].filter(Boolean))).join(" ");`                                                                                                     | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:430`             | numeric-operation           | `const current = Number(await env.RATE_LIMIT.get(key) \|\| "0");`                                                                                                                                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:497`             | numeric-operation           | `const arr = Array.isArray(parsed) ? parsed : Object.entries(parsed).map(([userid, value]) => ({ userid, ...value \|\| {} }));`                                                                     | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:498`             | numeric-operation           | `return arr.map((u) => ({`                                                                                                                                                                          | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:503`             | numeric-operation           | `})).filter((u) => u.userid && ["manager", "staff"].includes(u.role) && u.hash);`                                                                                                                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:760`             | numeric-operation           | `.filter((ch) => {`                                                                                                                                                                                 | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:779`             | numeric-operation           | `const num = Number(value);`                                                                                                                                                                        | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:781`             | numeric-operation           | `return Math.min(max, Math.max(min, Math.round(num * 100) / 100));`                                                                                                                                 | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:835`             | numeric-operation           | `const payments = Array.isArray(customer.payments) ? customer.payments.slice(0, 240).map((payment) => {`                                                                                            | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:844`             | numeric-operation           | `}).filter(Boolean) : [];`                                                                                                                                                                          | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:970`             | numeric-operation           | `total = Number(data.pages \|\| 1);`                                                                                                                                                                | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:985`             | numeric-operation           | `].filter((v)=>v !== void 0 && v !== null && String(v).trim()).join(" ");`                                                                                                                          | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1020`            | frontend-submitted-total    | `"operator_id","operator_name","cash_handover","bank_transfer_total","bank_transfer_count",`                                                                                                        | Backend must shadow-recompute these totals before they become accounting authority.    |
| Worker backend    | `deploy-worker/src/index.js:1021`            | frontend-submitted-total    | `"gross_received","handover_status","exported_at","export_text","source",`                                                                                                                          | Backend must shadow-recompute these totals before they become accounting authority.    |
| Worker backend    | `deploy-worker/src/index.js:1033`            | numeric-operation           | `return new Set((r.results\|\|[]).map(x=>x.name));`                                                                                                                                                 | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1067`            | frontend-submitted-total    | `await empAddColumn(env,"sessions","cash_handover","REAL");`                                                                                                                                        | Backend must shadow-recompute these totals before they become accounting authority.    |
| Worker backend    | `deploy-worker/src/index.js:1068`            | frontend-submitted-total    | `await empAddColumn(env,"sessions","bank_transfer_total","REAL");`                                                                                                                                  | Backend must shadow-recompute these totals before they become accounting authority.    |
| Worker backend    | `deploy-worker/src/index.js:1069`            | frontend-submitted-total    | `await empAddColumn(env,"sessions","bank_transfer_count","INTEGER");`                                                                                                                               | Backend must shadow-recompute these totals before they become accounting authority.    |
| Worker backend    | `deploy-worker/src/index.js:1070`            | frontend-submitted-total    | `await empAddColumn(env,"sessions","gross_received","REAL");`                                                                                                                                       | Backend must shadow-recompute these totals before they become accounting authority.    |
| Worker backend    | `deploy-worker/src/index.js:1205`            | numeric-operation           | `const y=Number(m[1]),mo=Number(m[2]),d=Number(m[3]);`                                                                                                                                              | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1225`            | numeric-operation           | `dt.setUTCDate(dt.getUTCDate()+Number(days\|\|0));`                                                                                                                                                 | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1234`            | numeric-operation           | `dt.setUTCMonth(dt.getUTCMonth()+Number(months\|\|0));`                                                                                                                                             | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1252`            | numeric-operation           | `return Math.max(0,Math.round((bm-am)/86400000));`                                                                                                                                                  | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1263`            | numeric-operation           | `await env.DB.prepare(`INSERT OR REPLACE INTO ${table} (${names.join(",")}) VALUES (${names.map(()=>"?").join(",")})`).bind(...vals).run();`                                                        | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1279`            | numeric-operation           | `return Number(row?.balance\|\|0);`                                                                                                                                                                 | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1287`            | numeric-operation           | `if(existing)return {ledger_id:existing.ledger_id,balance_after:Number(existing.balance_after\|\|0),delta:Number(existing.delta\|\|0),duplicate:true};`                                             | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1290`            | numeric-operation           | `const delta=Number(move.delta\|\|0);`                                                                                                                                                              | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1291`            | numeric-operation           | `const balanceAfter=Math.max(0,Math.round((before+delta)*100)/100);`                                                                                                                                | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1295`            | numeric-operation           | `bed:move.bed\|\|"",entry_id:move.entry_id\|\|"",type:move.type\|\|"",amount:Number(move.amount\|\|Math.abs(delta)\|\|0),`                                                                          | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1311`            | numeric-operation           | `const actual=Math.round(Number(paidRow?.total_paid\|\|0)*100)/100;`                                                                                                                                | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1312`            | numeric-operation           | `const target=Number(task.arrear_amount\|\|0);`                                                                                                                                                     | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1326`            | numeric-operation           | `const remain=Math.max(0,Math.round((target-actual)*100)/100);`                                                                                                                                     | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1389`            | numeric-operation           | `const value=Number(cfg[key]);`                                                                                                                                                                     | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1390`            | numeric-operation           | `return Number.isFinite(value)&&value>0?Math.round(value*100)/100:0;`                                                                                                                               | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1423`            | numeric-operation           | `let amount=Number(String(entry.amount\|\|0).replace(/,/g,""));`                                                                                                                                    | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1440`            | numeric-operation           | `let due=Number(String(entry.due\|\|0).replace(/,/g,""));`                                                                                                                                          | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1441`            | numeric-operation           | `let paid=Number(String(entry.paid\|\|0).replace(/,/g,""));`                                                                                                                                        | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1442`            | numeric-operation           | `let periodDue=Number(String(entry.period_due\|\|due\|\|0).replace(/,/g,""));`                                                                                                                      | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1447`            | numeric-operation           | `let listPrice=Number(String(entry.list_price\|\|0).replace(/,/g,""));`                                                                                                                             | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1449`            | numeric-operation           | `const periodDays=Number(entry.period_day_count\|\|0);`                                                                                                                                             | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1453`            | numeric-operation           | `const promiseAmount=Number(String(entry.promise_amount\|\|0).replace(/,/g,""));`                                                                                                                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1478`            | numeric-operation           | `periodDue=Math.round(periodDays*40*100)/100;`                                                                                                                                                      | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1516`            | numeric-operation           | `const remain=Math.max(0,Number(apTaskForPayment.arrear_amount\|\|0)-Number(apTaskForPayment.actual_received\|\|0));`                                                                               | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1519`            | numeric-operation           | `periodDue=Number(String(entry.period_due\|\|amount).replace(/,/g,""));`                                                                                                                            | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1531`            | numeric-operation           | `const depositHeldInput=Number(String(entry.deposit_held\|\|0).replace(/,/g,""));`                                                                                                                  | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1532`            | numeric-operation           | `const depositDeduction=Number(String(entry.deposit_deduction\|\|0).replace(/,/g,""));`                                                                                                             | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1548`            | frontend-submitted-total    | `cash_handover:Number(String(session.cash_handover\|\|0).replace(/,/g,"")),`                                                                                                                        | Backend must shadow-recompute these totals before they become accounting authority.    |
| Worker backend    | `deploy-worker/src/index.js:1548`            | numeric-operation           | `cash_handover:Number(String(session.cash_handover\|\|0).replace(/,/g,"")),`                                                                                                                        | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1548`            | backend-session-total-parse | `cash_handover:Number(String(session.cash_handover\|\|0).replace(/,/g,"")),`                                                                                                                        | Legacy Worker parses submitted totals; P0-003 must compare against backend recompute.  |
| Worker backend    | `deploy-worker/src/index.js:1549`            | frontend-submitted-total    | `bank_transfer_total:Number(String(session.bank_transfer_total\|\|0).replace(/,/g,"")),`                                                                                                            | Backend must shadow-recompute these totals before they become accounting authority.    |
| Worker backend    | `deploy-worker/src/index.js:1549`            | numeric-operation           | `bank_transfer_total:Number(String(session.bank_transfer_total\|\|0).replace(/,/g,"")),`                                                                                                            | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1549`            | backend-session-total-parse | `bank_transfer_total:Number(String(session.bank_transfer_total\|\|0).replace(/,/g,"")),`                                                                                                            | Legacy Worker parses submitted totals; P0-003 must compare against backend recompute.  |
| Worker backend    | `deploy-worker/src/index.js:1550`            | frontend-submitted-total    | `bank_transfer_count:Number(session.bank_transfer_count\|\|0),`                                                                                                                                     | Backend must shadow-recompute these totals before they become accounting authority.    |
| Worker backend    | `deploy-worker/src/index.js:1550`            | numeric-operation           | `bank_transfer_count:Number(session.bank_transfer_count\|\|0),`                                                                                                                                     | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1550`            | backend-session-total-parse | `bank_transfer_count:Number(session.bank_transfer_count\|\|0),`                                                                                                                                     | Legacy Worker parses submitted totals; P0-003 must compare against backend recompute.  |
| Worker backend    | `deploy-worker/src/index.js:1551`            | frontend-submitted-total    | `gross_received:Number(String(session.gross_received\|\|0).replace(/,/g,"")),`                                                                                                                      | Backend must shadow-recompute these totals before they become accounting authority.    |
| Worker backend    | `deploy-worker/src/index.js:1551`            | numeric-operation           | `gross_received:Number(String(session.gross_received\|\|0).replace(/,/g,"")),`                                                                                                                      | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1551`            | backend-session-total-parse | `gross_received:Number(String(session.gross_received\|\|0).replace(/,/g,"")),`                                                                                                                      | Legacy Worker parses submitted totals; P0-003 must compare against backend recompute.  |
| Worker backend    | `deploy-worker/src/index.js:1567`            | numeric-operation           | `excess:Number(entry.excess\|\|Math.max(0,amount-due)),excess_to:cleanText(entry.excess_to,40),bank_ref:cleanText(entry.bank_ref,80),`                                                              | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1569`            | numeric-operation           | `deposit_held:type==="CO"?depositBalance:depositHeldInput,deposit_return:cleanText(entry.deposit_return,20),deposit_amt:Number(String(entry.deposit_amt\|\|0).replace(/,/g,"")),`                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1570`            | numeric-operation           | `deposit_deduction:depositDeduction,ded_reason:cleanText(entry.ded_reason,80),ded_days:Number(entry.ded_days\|\|0),`                                                                                | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1571`            | numeric-operation           | `ded_rate:Number(String(entry.ded_rate\|\|40).replace(/,/g,"")),ded_note:cleanText(entry.ded_note,240),early_days:Number(entry.early_days\|\|0),`                                                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1594`            | numeric-operation           | `const totalPaid=Number(paidRow?.total_paid\|\|0);`                                                                                                                                                 | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1657`            | numeric-operation           | `return cleanMoney(Math.max(0,Number(task?.arrear_amount\|\|0)-Number(task?.actual_received\|\|0)));`                                                                                               | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1704`            | numeric-operation           | `const tasks=(taskRows.results\|\|[]).filter(t=>empCloseStatusIsOpen(t.close_status)&&empTaskRemaining(t)>0);`                                                                                      | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1705`            | numeric-operation           | `const seenIds=new Set(tasks.map(t=>cleanText(t.task_id,100)).filter(Boolean));`                                                                                                                    | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1706`            | numeric-operation           | `const seenKeys=new Set(tasks.map(t=>[`                                                                                                                                                             | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1735`            | numeric-operation           | `return json(tasks.map(empTaskToBossArrear).filter(a=>a.remain>0));`                                                                                                                                | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1779`            | numeric-operation           | `const patchAmount=Number(String(patch.arrear_amount\|\|0).replace(/,/g,""));`                                                                                                                      | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1802`            | numeric-operation           | `const amount=Number(fallback?.remain??patch.arrear_amount??0);`                                                                                                                                    | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1855`            | numeric-operation           | `const illegal=Object.keys(patch).filter(k=>!staffAllowed.has(k));`                                                                                                                                 | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:1860`            | numeric-operation           | `const remain=Math.max(0,Number(old.arrear_amount\|\|0)-Number(old.actual_received\|\|0));`                                                                                                         | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:2041`            | numeric-operation           | `cardEnd: Number(account.cardEnd \|\| 0),`                                                                                                                                                          | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:2042`            | numeric-operation           | `createdAt: Number(account.createdAt \|\| Date.now()),`                                                                                                                                             | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:2093`            | numeric-operation           | `customers: customers.map(sanitizeCustomer).filter(Boolean),`                                                                                                                                       | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:2107`            | numeric-operation           | `const customers = raw.map(sanitizeCustomer).filter(Boolean);`                                                                                                                                      | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:2169`            | numeric-operation           | `const num = Number(value);`                                                                                                                                                                        | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:2171`            | numeric-operation           | `clean[key] = Math.round(num * 100) / 100;`                                                                                                                                                         | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:2202`            | numeric-operation           | `const entries = session.entries.map(sanitizeEntry).filter(Boolean);`                                                                                                                               | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:2206`            | numeric-operation           | `const entryIds = new Set(entries.map((e) => e.id));`                                                                                                                                               | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Worker backend    | `deploy-worker/src/index.js:2207`            | numeric-operation           | `const arrears = Array.isArray(sa) ? sa.slice(0, MAX_SESSION_ENTRIES).map((a) => sanitizeArrear(a, sessionId, entryIds)).filter(Boolean) : [];`                                                     | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:1891` | numeric-operation           | `const num=v=>Number(String(v\|\|'').replace(/,/g,''));`                                                                                                                                            | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:1892` | numeric-operation           | `const fmtMoney=n=>(Number(n)\|\|0).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});`                                                                                     | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:1960` | frontend-submitted-total    | `cash_handover:s.cashBalance,`                                                                                                                                                                      | Backend must shadow-recompute these totals before they become accounting authority.    |
| Employee frontend | `deploy-worker/public/employee-v3.html:1961` | frontend-submitted-total    | `bank_transfer_total:s.bankIn,`                                                                                                                                                                     | Backend must shadow-recompute these totals before they become accounting authority.    |
| Employee frontend | `deploy-worker/public/employee-v3.html:1962` | frontend-submitted-total    | `bank_transfer_count:s.bankCount,`                                                                                                                                                                  | Backend must shadow-recompute these totals before they become accounting authority.    |
| Employee frontend | `deploy-worker/public/employee-v3.html:1963` | frontend-submitted-total    | `gross_received:s.grossReceived,`                                                                                                                                                                   | Backend must shadow-recompute these totals before they become accounting authority.    |
| Employee frontend | `deploy-worker/public/employee-v3.html:2046` | numeric-operation           | `const v=Number(state.rentConfig[key]);`                                                                                                                                                            | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2049` | numeric-operation           | `function addMonths(dateStr,n){const d=new Date(dateStr+'T00:00:00');const day=d.getDate();d.setMonth(d.getMonth()+Number(n\|\|0));if(d.getDate()!==day)d.setDate(0);return `${d.getFul`            | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2050` | numeric-operation           | `function addDays(dateStr,n){const d=new Date(dateStr+'T00:00:00');d.setDate(d.getDate()+Number(n\|\|0));return `${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`}`                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2051` | numeric-operation           | `function daysBetween(a,b){if(!a\|\|!b)return 0;return Math.max(0,Math.round((new Date(b+'T00:00:00')-new Date(a+'T00:00:00'))/86400000))}`                                                         | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2053` | numeric-operation           | `function findDateText(s){s=String(s\|\|'');const full=s.match(/(20\d{2})[-/.年](\d{1,2})[-/.月](\d{1,2})/);if(full)return `${full[1]}-${pad(full[2])}-${pad(full[3])}`;const md=s.match(`          | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2054` | numeric-operation           | `function dateFromAny(v){if(!v)return '';if(typeof v==='number'\|\|/^\d{10,13}$/.test(String(v))){const n=Number(v);const d=new Date(n<1e12?n*1000:n);if(!isNaN(d))return `${d.getFullY`            | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2063` | numeric-operation           | `const m=Number(c[1]),d=Number(c[2]);`                                                                                                                                                              | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2093` | numeric-operation           | `return parts.map(v=>String(v??'').trim()).filter(Boolean).filter(v=>{`                                                                                                                             | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2101` | numeric-operation           | `return uniqueTextParts(keys.map(k=>raw?.[k]));`                                                                                                                                                    | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2122` | numeric-operation           | `return [t?.bed,t?.tenant_name,t?.arrear_reason,t?.owner_note,t?.staff_note].map(v=>String(v\|\|'').trim()).filter(Boolean).join(' ');`                                                             | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2136` | numeric-operation           | `const text=[room,cardText,remark,humanText].filter(Boolean).join(' ');`                                                                                                                            | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2144` | numeric-operation           | `const fullCardText=[remark,cardText,humanText,room].filter(Boolean).join(' ');`                                                                                                                    | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2151` | numeric-operation           | `const exact=state.lockCards.filter(c=>String(c.bed)===bed);`                                                                                                                                       | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2152` | numeric-operation           | `const loose=state.lockCards.filter(c=>new RegExp(`(^\|\\D)${bed}(\\D\|$)`).test(String(c.searchText\|\|'')));`                                                                                     | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2188` | numeric-operation           | `].map(([zh,en,v,cls])=>`<div class="${cls\|\|''}"><span>${zh}<small>${en}</small></span><b>${esc(v)}</b></div>`).join('');`                                                                        | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2191` | numeric-operation           | `try{const r=await apiFetch('/api/employee/lock/cards');if(!r.ok)throw new Error('HTTP '+r.status);const data=await r.json();const list=data.roomsData?Object.entries(data.roomsData)`              | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2198` | numeric-operation           | `const open=state.tasks.filter(t=>!['PAID','CLEARED','CLOSED','VOID','已结清','结清','作废','WRITTEN_OFF','WAIVED'].includes(String(t.close_status\|\|''))&&taskRemain(t)>0);`                      | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2199` | numeric-operation           | `sel.innerHTML='<option value="">请选择要偿还的欠款</option>'+open.map(t=>`<option value="${esc(t.task_id)}">#${esc(t.bed\|\|'')} ${esc(t.tenant_name\|\|'-')} · ${fmtMoney(taskRemain(t))} AED · ` | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2242` | numeric-operation           | `return state.tasks.filter(t=>!['PAID','CLEARED','CLOSED','VOID','已结清','结清','作废','WRITTEN_OFF','WAIVED'].includes(String(t.close_status\|\|''))&&taskRemain(t)>0&&((bed&&String(t.bed\|\|`   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2290` | numeric-operation           | `const cashIn=rows.filter(e=>e.pay_type==='C'&&entryDirection(e)==='in').reduce((s,e)=>s+num(e.amount),0);`                                                                                         | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2291` | numeric-operation           | `const bankIn=rows.filter(e=>e.pay_type==='B'&&entryDirection(e)==='in').reduce((s,e)=>s+num(e.amount),0);`                                                                                         | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2292` | numeric-operation           | `const bankCount=rows.filter(e=>e.pay_type==='B'&&entryDirection(e)==='in'&&num(e.amount)>0).length;`                                                                                               | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2293` | numeric-operation           | `const rentIn=rows.filter(e=>e.type==='R').reduce((s,e)=>s+num(e.paid\|\|e.amount),0);`                                                                                                             | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2294` | numeric-operation           | `const depositIn=rows.filter(e=>e.type==='D').reduce((s,e)=>s+num(e.amount),0);`                                                                                                                    | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2295` | numeric-operation           | `const arrearsIn=rows.filter(e=>e.type==='AP').reduce((s,e)=>s+num(e.amount),0);`                                                                                                                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2296` | numeric-operation           | `const transferFee=rows.filter(e=>e.type==='TF'\|\|e.type==='TFF').reduce((s,e)=>s+num(e.amount),0);`                                                                                               | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2297` | numeric-operation           | `const depRefund=rows.filter(e=>e.type==='DR').reduce((s,e)=>s+num(e.amount),0);`                                                                                                                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2298` | numeric-operation           | `const expenses=rows.filter(e=>e.type==='E').reduce((s,e)=>s+num(e.amount),0);`                                                                                                                     | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2299` | numeric-operation           | `const cashOut=rows.filter(e=>e.pay_type==='C'&&entryDirection(e)==='out').reduce((s,e)=>s+num(e.amount),0);`                                                                                       | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2300` | numeric-operation           | `const bankOut=rows.filter(e=>e.pay_type==='B'&&entryDirection(e)==='out').reduce((s,e)=>s+num(e.amount),0);`                                                                                       | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2304` | numeric-operation           | `const localOnly=rows.filter(e=>e.sync_status!=='SYNCED').length;`                                                                                                                                  | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2319` | numeric-operation           | `$('sessionKpis').innerHTML=cards.map(([cn,en,v,cls,note])=>`<div class="kpi-card"><span>${cn}</span><small>${en}</small><b class="${cls}">${fmtDisplayMoney(v)}</b><small>${note}</s`              | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2330` | numeric-operation           | `state.drafts=state.drafts.filter(e=>e.id!==id);`                                                                                                                                                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2340` | numeric-operation           | `$('sessionPreview').innerHTML=rows.map((e,i)=>{`                                                                                                                                                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2345` | numeric-operation           | `const detail=[eventLabel(e.type),e.tenant_name\|\|'',e.pay_type==='B'?'银行':'现金',e.period_start&&e.period_end?`${e.period_start}→${e.period_end}`:''].filter(Boolean).join(' · ');`             | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2425` | numeric-operation           | `$('entrySummary').innerHTML=items.map(([k,v])=>`<div><span>${esc(k)}</span><b>${esc(v)}</b></div>`).join('');`                                                                                     | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2524` | numeric-operation           | `$('validationBox').innerHTML=[...errors.map(x=>`<div class="bad">阻断：${esc(x)}</div>`),...warns.map(x=>`<div class="warn">提醒：${esc(x)}</div>`),(!errors.length&&!warns.length?'<div>校`       | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2622` | numeric-operation           | `return state.tasks.filter(t=>!['PAID','CLEARED','CLOSED','VOID','已结清','结清','作废','WRITTEN_OFF','WAIVED'].includes(String(t.close_status\|\|''))&&taskRemain(t)>0);`                          | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2625` | numeric-operation           | `return state.lockCards.map(c=>{`                                                                                                                                                                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2656` | numeric-operation           | `}).filter(Boolean);`                                                                                                                                                                               | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2664` | numeric-operation           | `return openArrearTasks().map(t=>{`                                                                                                                                                                 | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2697` | numeric-operation           | `}).filter(Boolean);`                                                                                                                                                                               | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2701` | numeric-operation           | `const historyBeds=new Set(history.map(x=>String(x.bed\|\|'')).filter(Boolean));`                                                                                                                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2702` | numeric-operation           | `const items=[...dueFromLockCards().filter(x=>!historyBeds.has(String(x.bed\|\|''))),...history];`                                                                                                  | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2703` | numeric-operation           | `items.sort((a,b)=>(Number(b.force)-Number(a.force))\|\|(b.overdue_days-a.overdue_days)\|\|(b.amount-a.amount));`                                                                                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2711` | numeric-operation           | `return [bed,remark].filter(Boolean).join(' · ')\|\|'-';`                                                                                                                                           | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2724` | numeric-operation           | `...editableStatuses.map(s=>`<option value="${s}" ${s===currentStatus?'selected':''}>${s}</option>`)`                                                                                               | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2757` | numeric-operation           | `const history=items.filter(x=>x.source==='history'\|\|x.source==='history_unmatched');`                                                                                                            | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2758` | numeric-operation           | `const due=items.filter(x=>x.source==='ttlock');`                                                                                                                                                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2759` | numeric-operation           | `const force=items.filter(x=>x.force).length;`                                                                                                                                                      | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2760` | numeric-operation           | `const total=items.reduce((s,x)=>s+num(x.amount),0);`                                                                                                                                               | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2771` | numeric-operation           | `$('taskList').innerHTML=dashboard+`<div class="followup-list">${items.map(followupCard).join('')}</div>`;`                                                                                         | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2876` | numeric-operation           | `const section=(title,rows)=>{if(rows.length)lines.push('',`==== ${title} ====`,...rows.map(exportLine));};`                                                                                        | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2877` | numeric-operation           | `section('CASH RECEIVED',drafts.filter(e=>e.pay_type==='C'&&['R','D','TF','TFF'].includes(e.type)&&num(e.amount)>0));`                                                                              | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2878` | numeric-operation           | `section('BANK RECEIVED',drafts.filter(e=>e.pay_type==='B'&&['R','D','TF','TFF'].includes(e.type)&&num(e.amount)>0));`                                                                              | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2879` | numeric-operation           | `section('ARREAR REPAID',drafts.filter(e=>e.type==='AP'));`                                                                                                                                         | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2880` | numeric-operation           | `section('CHECKOUT',drafts.filter(e=>e.type==='CO'));`                                                                                                                                              | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2881` | numeric-operation           | `section('DEPOSIT RETURN',drafts.filter(e=>e.type==='DR'));`                                                                                                                                        | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2882` | numeric-operation           | `section('EXPENSE',drafts.filter(e=>e.type==='E'));`                                                                                                                                                | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2883` | numeric-operation           | `section('TRANSFER',drafts.filter(e=>e.type==='TF'));`                                                                                                                                              | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2884` | numeric-operation           | `const opened=drafts.filter(e=>e.type==='R'&&e.entry_clr!=='Y'&&num(e.period_due)>num(e.paid));`                                                                                                    | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2889` | numeric-operation           | `const cashIn=drafts.filter(e=>e.pay_type==='C'&&!['E','DR'].includes(e.type)).reduce((s,e)=>s+num(e.amount),0);`                                                                                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2890` | numeric-operation           | `const bankIn=drafts.filter(e=>e.pay_type==='B'&&!['E','DR'].includes(e.type)).reduce((s,e)=>s+num(e.amount),0);`                                                                                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2891` | numeric-operation           | `const cashOut=drafts.filter(e=>e.pay_type==='C'&&['E','DR'].includes(e.type)).reduce((s,e)=>s+num(e.amount),0);`                                                                                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2892` | numeric-operation           | `const bankOut=drafts.filter(e=>e.pay_type==='B'&&['E','DR'].includes(e.type)).reduce((s,e)=>s+num(e.amount),0);`                                                                                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2893` | numeric-operation           | `const rentIn=drafts.filter(e=>e.type==='R').reduce((s,e)=>s+num(e.paid\|\|e.amount),0);`                                                                                                           | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2894` | numeric-operation           | `const depositIn=drafts.filter(e=>e.type==='D').reduce((s,e)=>s+num(e.amount),0);`                                                                                                                  | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2895` | numeric-operation           | `const arrearsIn=drafts.filter(e=>e.type==='AP').reduce((s,e)=>s+num(e.amount),0);`                                                                                                                 | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2896` | numeric-operation           | `const feeIn=drafts.filter(e=>e.type==='TF'\|\|e.type==='TFF').reduce((s,e)=>s+num(e.amount),0);`                                                                                                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2897` | numeric-operation           | `const bankCount=drafts.filter(e=>e.pay_type==='B'&&!['E','DR'].includes(e.type)&&num(e.amount)>0).length;`                                                                                         | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2899` | numeric-operation           | `lines.push('','==== SUMMARY ====',`Rent income: ${fmtMoney(rentIn)}`,`Deposit received: ${fmtMoney(depositIn)}`,`Arrears repaid: ${fmtMoney(arrearsIn)}`,`Transfer fee: ${fmtMoney(f`              | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2939` | numeric-operation           | `</div>${state.drafts.map((e,i)=>previewCard(e,i)).join('')}`;`                                                                                                                                     | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2953` | numeric-operation           | `const btns=[$('btnExportSession'),$('btnBuildExport')].filter(Boolean);`                                                                                                                           | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2955` | numeric-operation           | `const oldLabels=btns.map(b=>[b,b.innerHTML]);`                                                                                                                                                     | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:2957` | numeric-operation           | `const pending=state.drafts.filter(e=>e.sync_status!=='SYNCED');`                                                                                                                                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:3048` | numeric-operation           | `].map(([v,zh,en])=>`<option value="${v}">${zh} ${en}</option>`).join('');`                                                                                                                         | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:3100` | numeric-operation           | `grid.innerHTML=`<div class="kpi-section-label">交接核心<small>HANDOVER CORE</small></div><div class="handover-anchors">${anchors.map(card).join('')}</div><div class="kpi-section-label"`          | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:3131` | numeric-operation           | `].map(([v,zh,en])=>`<option value="${v}">${zh} ${en}</option>`).join('');`                                                                                                                         | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `deploy-worker/public/employee-v3.html:3167` | numeric-operation           | `const arrearTotal=open.reduce((s,t)=>s+taskRemain(t),0);`                                                                                                                                          | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:1891`                      | numeric-operation           | `const num=v=>Number(String(v\|\|'').replace(/,/g,''));`                                                                                                                                            | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:1892`                      | numeric-operation           | `const fmtMoney=n=>(Number(n)\|\|0).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});`                                                                                     | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:1960`                      | frontend-submitted-total    | `cash_handover:s.cashBalance,`                                                                                                                                                                      | Backend must shadow-recompute these totals before they become accounting authority.    |
| Employee frontend | `employee-v3.html:1961`                      | frontend-submitted-total    | `bank_transfer_total:s.bankIn,`                                                                                                                                                                     | Backend must shadow-recompute these totals before they become accounting authority.    |
| Employee frontend | `employee-v3.html:1962`                      | frontend-submitted-total    | `bank_transfer_count:s.bankCount,`                                                                                                                                                                  | Backend must shadow-recompute these totals before they become accounting authority.    |
| Employee frontend | `employee-v3.html:1963`                      | frontend-submitted-total    | `gross_received:s.grossReceived,`                                                                                                                                                                   | Backend must shadow-recompute these totals before they become accounting authority.    |
| Employee frontend | `employee-v3.html:2046`                      | numeric-operation           | `const v=Number(state.rentConfig[key]);`                                                                                                                                                            | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2049`                      | numeric-operation           | `function addMonths(dateStr,n){const d=new Date(dateStr+'T00:00:00');const day=d.getDate();d.setMonth(d.getMonth()+Number(n\|\|0));if(d.getDate()!==day)d.setDate(0);return `${d.getFul`            | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2050`                      | numeric-operation           | `function addDays(dateStr,n){const d=new Date(dateStr+'T00:00:00');d.setDate(d.getDate()+Number(n\|\|0));return `${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`}`                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2051`                      | numeric-operation           | `function daysBetween(a,b){if(!a\|\|!b)return 0;return Math.max(0,Math.round((new Date(b+'T00:00:00')-new Date(a+'T00:00:00'))/86400000))}`                                                         | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2053`                      | numeric-operation           | `function findDateText(s){s=String(s\|\|'');const full=s.match(/(20\d{2})[-/.年](\d{1,2})[-/.月](\d{1,2})/);if(full)return `${full[1]}-${pad(full[2])}-${pad(full[3])}`;const md=s.match(`          | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2054`                      | numeric-operation           | `function dateFromAny(v){if(!v)return '';if(typeof v==='number'\|\|/^\d{10,13}$/.test(String(v))){const n=Number(v);const d=new Date(n<1e12?n*1000:n);if(!isNaN(d))return `${d.getFullY`            | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2063`                      | numeric-operation           | `const m=Number(c[1]),d=Number(c[2]);`                                                                                                                                                              | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2093`                      | numeric-operation           | `return parts.map(v=>String(v??'').trim()).filter(Boolean).filter(v=>{`                                                                                                                             | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2101`                      | numeric-operation           | `return uniqueTextParts(keys.map(k=>raw?.[k]));`                                                                                                                                                    | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2122`                      | numeric-operation           | `return [t?.bed,t?.tenant_name,t?.arrear_reason,t?.owner_note,t?.staff_note].map(v=>String(v\|\|'').trim()).filter(Boolean).join(' ');`                                                             | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2136`                      | numeric-operation           | `const text=[room,cardText,remark,humanText].filter(Boolean).join(' ');`                                                                                                                            | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2144`                      | numeric-operation           | `const fullCardText=[remark,cardText,humanText,room].filter(Boolean).join(' ');`                                                                                                                    | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2151`                      | numeric-operation           | `const exact=state.lockCards.filter(c=>String(c.bed)===bed);`                                                                                                                                       | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2152`                      | numeric-operation           | `const loose=state.lockCards.filter(c=>new RegExp(`(^\|\\D)${bed}(\\D\|$)`).test(String(c.searchText\|\|'')));`                                                                                     | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2188`                      | numeric-operation           | `].map(([zh,en,v,cls])=>`<div class="${cls\|\|''}"><span>${zh}<small>${en}</small></span><b>${esc(v)}</b></div>`).join('');`                                                                        | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2191`                      | numeric-operation           | `try{const r=await apiFetch('/api/employee/lock/cards');if(!r.ok)throw new Error('HTTP '+r.status);const data=await r.json();const list=data.roomsData?Object.entries(data.roomsData)`              | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2198`                      | numeric-operation           | `const open=state.tasks.filter(t=>!['PAID','CLEARED','CLOSED','VOID','已结清','结清','作废','WRITTEN_OFF','WAIVED'].includes(String(t.close_status\|\|''))&&taskRemain(t)>0);`                      | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2199`                      | numeric-operation           | `sel.innerHTML='<option value="">请选择要偿还的欠款</option>'+open.map(t=>`<option value="${esc(t.task_id)}">#${esc(t.bed\|\|'')} ${esc(t.tenant_name\|\|'-')} · ${fmtMoney(taskRemain(t))} AED · ` | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2242`                      | numeric-operation           | `return state.tasks.filter(t=>!['PAID','CLEARED','CLOSED','VOID','已结清','结清','作废','WRITTEN_OFF','WAIVED'].includes(String(t.close_status\|\|''))&&taskRemain(t)>0&&((bed&&String(t.bed\|\|`   | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2290`                      | numeric-operation           | `const cashIn=rows.filter(e=>e.pay_type==='C'&&entryDirection(e)==='in').reduce((s,e)=>s+num(e.amount),0);`                                                                                         | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2291`                      | numeric-operation           | `const bankIn=rows.filter(e=>e.pay_type==='B'&&entryDirection(e)==='in').reduce((s,e)=>s+num(e.amount),0);`                                                                                         | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2292`                      | numeric-operation           | `const bankCount=rows.filter(e=>e.pay_type==='B'&&entryDirection(e)==='in'&&num(e.amount)>0).length;`                                                                                               | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2293`                      | numeric-operation           | `const rentIn=rows.filter(e=>e.type==='R').reduce((s,e)=>s+num(e.paid\|\|e.amount),0);`                                                                                                             | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2294`                      | numeric-operation           | `const depositIn=rows.filter(e=>e.type==='D').reduce((s,e)=>s+num(e.amount),0);`                                                                                                                    | Numeric reductions must be reviewed before totals are treated as accounting authority. |
| Employee frontend | `employee-v3.html:2295`                      | numeric-operation           | `const arrearsIn=rows.filter(e=>e.type==='AP').reduce((s,e)=>s+num(e.amount),0);`                                                                                                                   | Numeric reductions must be reviewed before totals are treated as accounting authority. |

## Boundary

The current production route is not changed by this scan. P0-003 remains Partial until backend recomputation becomes the authoritative response/write path and is covered by authenticated regression tests.
