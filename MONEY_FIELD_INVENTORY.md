# Money Field Inventory

Generated: 2026-05-24, Asia/Dubai

Scope: P0-001A inventory only. This document maps current money fields and precision risks without changing business formulas, live write paths, dashboard calculations, handover logic, or database schema.

## Inventory

| Area                | File                                              | Field / Variable                                                     | Type / Storage                            | Source                             | Used For                        | Risk                                                            | Recommendation                                                             |
| ------------------- | ------------------------------------------------- | -------------------------------------------------------------------- | ----------------------------------------- | ---------------------------------- | ------------------------------- | --------------------------------------------------------------- | -------------------------------------------------------------------------- |
| Database field      | `migrations/local/001_clean_legacy_bootstrap.sql` | `sessions.cash_handover`                                             | `REAL`                                    | Employee session summary           | Cash handover total             | Floating storage for accounting authority                       | Add `cash_handover_fils INTEGER` in P0-001C, dual-write, reconcile legacy. |
| Database field      | `migrations/local/001_clean_legacy_bootstrap.sql` | `sessions.bank_transfer_total`                                       | `REAL`                                    | Employee session summary           | Bank transfer total             | Floating storage                                                | Add `bank_transfer_total_fils INTEGER`.                                    |
| Database field      | `migrations/local/001_clean_legacy_bootstrap.sql` | `sessions.gross_received`                                            | `REAL`                                    | Employee session summary           | Gross received                  | Floating storage and may trust frontend                         | Backend recompute in P0-003, store fils.                                   |
| Database field      | `migrations/local/001_clean_legacy_bootstrap.sql` | `transactions.amount`                                                | `REAL`                                    | Employee entry / owner import      | Main transaction amount         | Core accounting amount uses decimal float semantics             | Dual-write `amount_fils`, then migrate readers.                            |
| Database field      | `migrations/local/001_clean_legacy_bootstrap.sql` | `transactions.due` / `paid` / `deficit`                              | `REAL`                                    | Employee entry                     | Rent due, paid, shortfall       | Short-pay math can drift                                        | Add `due_fils`, `paid_fils`, `deficit_fils`; backend recompute.            |
| Database field      | `migrations/local/001_clean_legacy_bootstrap.sql` | `transactions.dep_due` / `dep_paid` / `dep_def`                      | `REAL`                                    | Legacy owner entry                 | Deposit component               | Deposit/rent split can drift                                    | Replace with deposit ledger fils values after reconciliation.              |
| Database field      | `migrations/local/001_clean_legacy_bootstrap.sql` | `transactions.period_due`                                            | `REAL`                                    | Employee rent period               | Period receivable               | Receivable amount not first-class                               | Move to receivable amount fils in P0-008.                                  |
| Database field      | `migrations/local/001_clean_legacy_bootstrap.sql` | `transactions.deposit_held` / `deposit_amt` / `deposit_deduction`    | `REAL`                                    | Deposit refund / checkout          | Deposit balance and deduction   | Deposit liability precision risk                                | Store deposit ledger deltas as integer fils.                               |
| Database field      | `migrations/local/001_clean_legacy_bootstrap.sql` | `transactions.promise_amount`                                        | `REAL`                                    | Tail/arrear promise                | Follow-up amount                | Task promise may drift from receivable                          | Store `promise_amount_fils`; task is not accounting authority.             |
| Database field      | `migrations/local/001_clean_legacy_bootstrap.sql` | `arrears.remain`                                                     | `REAL`                                    | Legacy arrears                     | Unpaid balance                  | Legacy arrears not tied to formal receivable                    | Backfill to receivables + payments in P0-008.                              |
| Database field      | `migrations/local/001_clean_legacy_bootstrap.sql` | `arrear_tasks.arrear_amount` / `promise_amount` / `actual_received`  | `REAL`                                    | Employee follow-up                 | Operational collection tracking | Follow-up values can diverge from accounting ledger             | Add fils columns and link to receivable/payment records.                   |
| Database field      | `migrations/local/001_clean_legacy_bootstrap.sql` | `deposit_ledger.amount` / `delta` / `balance_after`                  | `REAL`                                    | Employee deposit movement          | Deposit liability ledger        | Ledger balances should never use float semantics                | Add `*_fils INTEGER`, reconcile before production migration.               |
| SQL migration       | `migrations/001_employee_anchor_schema.sql`       | `arrear_tasks.*amount*`                                              | `REAL`                                    | Anchor schema                      | Arrear task compatibility       | Legacy schema keeps float values                                | Leave as legacy; prepare `*_fils` migration draft later.                   |
| SQL draft           | `migration-drafts/002_commercial_bootstrap.sql`   | `*_fils` columns                                                     | `INTEGER`                                 | Commercial draft schema            | Future commercial ledger        | Good target but not live                                        | Promote only after migration rehearsal and human approval.                 |
| Worker write path   | `deploy-worker/src/index.js`                      | `amount`, `due`, `paid`, `deficit`                                   | JS `Number` then D1 `REAL`                | `/api/employee/entry`              | Employee financial entry        | Backend parses with `Number(...)`; precision and authority risk | P0-001B/C should normalize with money helper and dual-write fils.          |
| Worker write path   | `deploy-worker/src/index.js`                      | `session.cash_handover`, `bank_transfer_total`, `gross_received`     | JS `Number` then D1 `REAL`                | Employee handover body             | Handover totals                 | Browser can be source of accounting totals                      | P0-003 backend recompute from accepted rows.                               |
| Worker write path   | `deploy-worker/src/index.js`                      | `empDepositMove.amount`, `delta`, `balance_after`                    | JS `Number` and D1 `REAL`                 | Deposit collection/refund/checkout | Deposit liability               | Deposit ledger should be auditable integer minor units          | Add `amount_fils`, `delta_fils`, `balance_after_fils`.                     |
| Worker write path   | `deploy-worker/src/index.js`                      | `arrear_amount`, `promise_amount`, `actual_received`                 | JS `Number` and D1 `REAL`                 | Arrear task update                 | Follow-up workflow              | Operational task may diverge from ledger                        | Link task to receivable and payments in P0-008.                            |
| Worker read path    | `deploy-worker/src/index.js`                      | `SUM(delta) AS balance`                                              | D1 aggregate over `REAL`                  | `/api/employee/deposit`            | Deposit balance lookup          | Floating aggregate                                              | Recompute from `delta_fils` after dual-write.                              |
| Worker read path    | `deploy-worker/src/index.js`                      | history/session detail amount fields                                 | D1 `REAL` returned as JSON                | Owner history APIs                 | Owner history display/export    | Response exposes legacy decimal numbers                         | API should return both `*_fils` and display strings after migration.       |
| API request body    | `employee-v3.html` -> `/api/employee/entry`       | `amount`, `due`, `paid`, `period_due`, `deposit_*`, `promise_amount` | Browser decimal strings/numbers           | Staff form                         | Entry submission                | Frontend computes and submits monetary authority                | Backend must parse strings and recompute authoritative fields.             |
| API response body   | `/api/rent_config`                                | rent map JSON                                                        | JSON number                               | Owner settings / employee context  | Rent reference by bed           | JSON numbers can become decimal authority                       | Store rent config as integer fils with effective dates.                    |
| API response body   | `/api/arrear_tasks`                               | `arrear_amount`, `promise_amount`, `actual_received`                 | JSON number                               | Follow-up list                     | Staff collection tasks          | Task values not immutable accounting facts                      | Add linked receivable/payment ids and fils values.                         |
| Employee form       | `employee-v3.html`                                | `#amount`, `#due`, `#paid`, `#periodDue`                             | DOM decimal                               | Staff input and computed values    | Entry capture                   | Uses `Number`, `Math.max`, `fmtMoney`                           | Keep as UI only; backend validates with helper.                            |
| Employee handover   | `employee-v3.html`                                | `cashHandover`, `bankIn`, `grossReceived`                            | JS `Number` reductions                    | Session summary/export             | Handover anchors                | Frontend totals can drift or be tampered                        | Backend recompute and return signed totals.                                |
| Owner dashboard     | `index-51-main.js`                                | `parseMoney`, `fmtMoney`, dashboard sums                             | JS `parseFloat` / `Number` / `Math.round` | Owner UI and TXT import            | Analytics and display           | Not authoritative but can mislead if used as accounting truth   | Owner UI should consume backend-computed totals.                           |
| Owner history       | `index-51-main.js` / `/api/history`               | `amount`, `due`, `paid`, `deficit`, deposit fields                   | Legacy decimal                            | Imported sessions and API display  | Historical analysis             | Legacy rows may contain float decimals                          | Backfill and reconciliation report before migration.                       |
| Delete-session void | `deploy-worker/src/index.js`                      | `transactions`, `deposit_ledger`, `arrears`, `arrear_tasks` amounts  | Preserved legacy rows                     | `/api/delete_session`              | Void audit state                | Amount rows are preserved but remain float legacy               | Keep void behavior; later add audit event with fils snapshot.              |
| App setting         | `app_settings.value` with key `rent_ref_room`     | JSON number map                                                      | Owner rent config                         | Rent due reference                 | Config amount source            | Current JSON has numeric AED values                             | Store canonical rent config in integer fils + effective date table.        |

## Static Scan Evidence

`npm run audit:money` generated `MONEY_PRECISION_AUDIT_RESULT.md`:

- `REAL_FLOAT_RISKS=179`
- `JS_NUMBER_PARSEFLOAT_RISKS=466`
- `FRONTEND_MONEY_CALC_RISKS=435`
- `BACKEND_MONEY_CALC_RISKS=143`
- `MONEY_FINDINGS=2546`

## Boundary

This inventory does not mean every occurrence is immediately wrong. It identifies where commercial accounting authority must eventually move to backend-owned integer minor units.
