# Finance Flow Map

Generated: 2026-05-24, Asia/Dubai

Scope: P0-001A money-flow map only. Current production behavior was not changed.

## Current Money Flows

| Flow                                | Frontend Source                                                 | API                                                                   | Backend Calculation                                                                        | Tables Written                                                                                        | Tables Read                                             | Current Precision Risk                                                         | Required Future Rule                                                                     |
| ----------------------------------- | --------------------------------------------------------------- | --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | ------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- |
| Employee rent collection            | `employee-v3.html` fields `amount`, `due`, `paid`, `period_due` | `POST /api/employee/entry`                                            | Worker parses legacy amount values with `Number(...)`, validates some deltas, writes entry | `sessions`, `transactions`, possible `arrear_tasks`, `entry_events`, `audit_logs`                     | `app_settings`, `arrear_tasks`, `deposit_ledger`        | Frontend computes period due/paid; Worker still uses JS Number and legacy REAL | Backend parses AED strings to fils, recomputes due/paid/deficit, dual-writes `*_fils`.   |
| Employee handover                   | Employee session summary/export                                 | `POST /api/employee/entry` per row; export remains frontend-generated | Session totals can be submitted from browser session context                               | `sessions` fields `cash_handover`, `bank_transfer_total`, `gross_received`                            | `transactions` for history                              | Browser can become accounting authority; totals stored as REAL                 | P0-003 backend recomputes handover totals from accepted rows in integer fils.            |
| Cash received                       | Employee pay type `C`                                           | `POST /api/employee/entry`                                            | Current live path stores entry amount and category                                         | `transactions`, `sessions` summary                                                                    | Owner history/dashboard                                 | `amount` as JS Number/REAL, cash handover calculated in frontend               | Store transaction amount fils; backend computes cash handover = cash in - cash out.      |
| Bank transfer received              | Employee pay type `B`                                           | `POST /api/employee/entry`                                            | Current live path stores amount and optional bank ref                                      | `transactions`, `sessions.bank_transfer_total`                                                        | Owner dashboard/history                                 | Bank total can be browser computed and REAL                                    | Backend computes bank total and count from accepted bank rows.                           |
| Rent config influence               | Owner rent config UI / API                                      | `GET/POST /api/rent_config`                                           | Worker stores JSON object in `app_settings.value`                                          | `app_settings`                                                                                        | Employee context, follow-up generation, rent due checks | JSON values are numbers and no effective date                                  | Store rent in `bed_rent_config_versions` as `rent_fils`, with effective dates.           |
| Deposit received                    | Employee `D` event                                              | `POST /api/employee/entry`                                            | Worker may call `empDepositMove` with amount/delta                                         | `transactions`, `deposit_ledger`                                                                      | `deposit_ledger` balance lookup                         | Deposit liability ledger uses REAL amount/delta/balance                        | Deposit ledger must be integer-fils liability ledger; transaction links to ledger event. |
| Deposit refund / checkout deduction | Employee `DR` / `CO` event                                      | `POST /api/employee/entry`                                            | Worker checks against deposit balance and writes negative delta                            | `transactions`, `deposit_ledger`                                                                      | `deposit_ledger` SUM(delta)                             | Balance is SUM over REAL, plus tolerances like `+0.01`                         | Use integer fils and explicit refund/deduction reason; no float tolerance.               |
| Arrears opened by short rent        | Employee `R` with amount < due                                  | `POST /api/employee/entry`                                            | Worker creates or updates `arrear_tasks` with remain/promise                               | `arrear_tasks`, `transactions`                                                                        | Existing `arrear_tasks`                                 | Tail amount computed with Number/REAL and not formal receivable                | P0-008 creates receivable first; arrear task references receivable id and amount fils.   |
| Arrears repayment                   | Employee `AP` event                                             | `POST /api/employee/entry`                                            | Worker applies actual received to task and may close it                                    | `transactions`, `arrear_tasks`                                                                        | `arrear_tasks`                                          | Repayment can close operational task without formal payment ledger             | Payment record must reduce receivable in integer fils; task status mirrors ledger.       |
| Refund / expense                    | Employee `DR`, `CO`, `E` events                                 | `POST /api/employee/entry`                                            | Current flow stores amount and category                                                    | `transactions`, `deposit_ledger` if deposit-related                                                   | Owner summary/history                                   | Cash out affects handover; REAL/Number risk                                    | Backend classifies direction and recomputes cash out in fils.                            |
| Owner dashboard statistics          | `index-51-main.js` and owner APIs                               | `GET /api/history`, local analysis                                    | Owner UI reduces amounts with `Number`, `parseFloat`, `Math.round`                         | Usually read-only; may import sessions separately                                                     | `sessions`, `transactions`, local state                 | UI math can become misleading if treated as authority                          | Dashboard should display backend-computed integer-fils summaries.                        |
| Owner history query                 | Owner history panel                                             | `GET /api/history`, `GET /api/session_detail`                         | Worker returns legacy rows                                                                 | none                                                                                                  | `sessions`, `transactions`                              | Legacy amount fields remain decimals                                           | Response should include `*_fils` and formatted display strings after migration.          |
| Delete-session void                 | Owner void action                                               | `POST /api/delete_session`                                            | P0-004 now marks rows voided and writes audit evidence                                     | `sessions`, `transactions`, `deposit_ledger`, `arrears`, `arrear_tasks`, `audit_logs`, `entry_events` | Same tables                                             | Original rows preserved but still legacy REAL                                  | Keep void immutable; add fils snapshots once dual-write exists.                          |
| App settings JSON                   | Owner config and local seed                                     | `GET/POST /api/rent_config`                                           | Stores `rent_ref_room` JSON                                                                | `app_settings`                                                                                        | Employee rent lookup                                    | JSON number precision and no versioning                                        | Effective-dated config table with integer-fils rent.                                     |

## Accounting Authority Assessment

| Question                                               | Current Answer                                                                                                         | Risk                                                        |
| ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| Which amounts are front-end calculated then submitted? | Employee `amount`, `due`, `paid`, `period_due`, session summary totals; owner dashboard reductions.                    | Browser can become accounting authority.                    |
| Which amounts are backend recalculated?                | Some checks exist for deposits, arrears, and configured rent, but live Worker still uses JS Number and legacy columns. | Partial validation is not enough for commercial accounting. |
| Which amounts enter database?                          | `sessions`, `transactions`, `deposit_ledger`, `arrears`, `arrear_tasks`, and `app_settings` values.                    | Most live legacy columns use `REAL` or JSON numbers.        |
| Which amounts enter owner statistics?                  | Session totals, transaction amount/due/paid/deficit, deposit ledger, arrears tasks, rent config.                       | Dashboard accuracy depends on legacy numeric values.        |
| Which items depend on P0-002/P0-003/P0-008?            | Handover atomicity, backend recompute, receivable lifecycle.                                                           | P0-001 cannot be fully closed until those are wired.        |
